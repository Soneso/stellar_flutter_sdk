name: Check for upstream XDR updates

on:
  schedule:
    # Runs daily at 08:00 UTC — adjust after initial testing
    - cron: '30 13 * * *'
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: read
  issues: write

jobs:
  check-xdr-updates:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Clone upstream stellar-xdr (curr branch)
        id: clone
        continue-on-error: true
        run: |
          git clone --depth 1 --branch curr \
            https://github.com/stellar/stellar-xdr.git /tmp/stellar-xdr

      - name: Compare .x files
        if: steps.clone.outcome == 'success'
        id: compare
        run: |
          shopt -s nullglob
          any_changed=false

          : > /tmp/changes_summary.txt
          : > /tmp/full_diff.txt

          for local_file in xdr/*.x; do
            filename=$(basename "$local_file")
            upstream_file="/tmp/stellar-xdr/$filename"

            if [ ! -f "$upstream_file" ]; then
              echo "::warning::Local file $filename not found upstream — may have been removed or renamed"
              continue
            fi

            if ! diff -q "$local_file" "$upstream_file" > /dev/null 2>&1; then
              any_changed=true
              added=$(diff "$local_file" "$upstream_file" | grep -c '^>' || true)
              removed=$(diff "$local_file" "$upstream_file" | grep -c '^<' || true)
              # shellcheck disable=SC2016
              printf -- '- `%s`: +%s / -%s lines\n' "$filename" "$added" "$removed" >> /tmp/changes_summary.txt

              # shellcheck disable=SC2016
              printf '\n<details>\n<summary>%s</summary>\n\n```diff\n' "$filename" >> /tmp/full_diff.txt
              diff -u "$local_file" "$upstream_file" \
                --label "local/$filename" --label "upstream/$filename" >> /tmp/full_diff.txt || true
              printf '\n```\n\n</details>\n' >> /tmp/full_diff.txt
            fi
          done

          # Check for new .x files upstream that we don't have yet
          for upstream_file in /tmp/stellar-xdr/*.x; do
            filename=$(basename "$upstream_file")
            if [ ! -f "xdr/$filename" ]; then
              any_changed=true
              lines=$(wc -l < "$upstream_file")
              # shellcheck disable=SC2016
              printf -- '- `%s`: **new file** (%s lines)\n' "$filename" "$lines" >> /tmp/changes_summary.txt
              printf '\n<details>\n<summary>%s (new)</summary>\n\nThis is a new XDR definition file not yet present in the SDK.\n\n</details>\n' "$filename" >> /tmp/full_diff.txt
            fi
          done

          echo "any_changed=$any_changed" >> "$GITHUB_OUTPUT"

          if [ "$any_changed" = true ]; then
            upstream_sha=$(git -C /tmp/stellar-xdr rev-parse HEAD)
            upstream_sha_short=$(git -C /tmp/stellar-xdr rev-parse --short HEAD)
            echo "$upstream_sha" > /tmp/upstream_sha.txt
            echo "$upstream_sha_short" > /tmp/upstream_sha_short.txt
          fi

      - name: Check for existing open issue
        if: steps.compare.outputs.any_changed == 'true'
        id: existing
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          issues=$(gh issue list \
            --label "xdr-update" \
            --state open \
            --json number)
          count=$(jq 'length' <<< "$issues")
          number=$(jq -r '.[0].number // empty' <<< "$issues")
          echo "open_issues=$count" >> "$GITHUB_OUTPUT"
          echo "issue_number=$number" >> "$GITHUB_OUTPUT"

      - name: Comment on existing issue if upstream advanced
        if: steps.compare.outputs.any_changed == 'true' && steps.existing.outputs.open_issues != '0'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ steps.existing.outputs.issue_number }}
        run: |
          if [ -z "$ISSUE_NUMBER" ]; then
            echo "::warning::Open xdr-update issue detected but its number could not be determined — skipping comment"
            exit 0
          fi

          upstream_sha=$(cat /tmp/upstream_sha.txt)
          upstream_sha_short=$(cat /tmp/upstream_sha_short.txt)

          # Skip when the issue body or an earlier comment already covers this
          # upstream commit. Only workflow-authored comments count as the dedup
          # key so a third-party comment cannot suppress the notification (gh
          # reports the Actions bot login as either github-actions or
          # app/github-actions depending on version).
          thread=$(gh issue view "$ISSUE_NUMBER" --json body,comments \
            --jq '.body + "\n" + ([.comments[] | select(.author.login == "github-actions" or .author.login == "app/github-actions") | .body] | join("\n"))')
          if grep -qF "$upstream_sha" <<< "$thread"; then
            echo "Issue #$ISSUE_NUMBER already references $upstream_sha_short — nothing to do"
            exit 0
          fi

          # shellcheck disable=SC2016,SC2129
          printf '%s\n' 'Upstream `curr` has advanced since this issue was last updated. Sync to the commit below instead of the one referenced above.' > /tmp/comment_body.md
          # shellcheck disable=SC2016,SC2129
          printf '\n**Upstream commit:** [`%s`](https://github.com/stellar/stellar-xdr/commit/%s)\n' "$upstream_sha_short" "$upstream_sha" >> /tmp/comment_body.md
          printf '\n## Changed files\n\n' >> /tmp/comment_body.md
          cat /tmp/changes_summary.txt >> /tmp/comment_body.md
          printf '\n## Diffs\n' >> /tmp/comment_body.md
          # Truncate diffs to stay under GitHub's 65536-char comment body limit
          max_diff_size=50000
          diff_size=$(wc -c < /tmp/full_diff.txt)
          if [ "$diff_size" -gt "$max_diff_size" ]; then
            # shellcheck disable=SC2129
            head -c "$max_diff_size" /tmp/full_diff.txt >> /tmp/comment_body.md
            printf '\n```\n\n</details>\n\n' >> /tmp/comment_body.md
            printf '> **Note:** Diffs truncated (%s bytes total). See the [upstream commit](https://github.com/stellar/stellar-xdr/commit/%s) for the full changeset.\n' "$diff_size" "$upstream_sha" >> /tmp/comment_body.md
          else
            cat /tmp/full_diff.txt >> /tmp/comment_body.md
          fi

          gh issue comment "$ISSUE_NUMBER" --body-file /tmp/comment_body.md

      - name: Ensure xdr-update label exists
        if: steps.compare.outputs.any_changed == 'true' && steps.existing.outputs.open_issues == '0'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh label create "xdr-update" \
            --description "Upstream XDR definition changes detected" \
            --color "1d76db" \
            2>/dev/null || true

      - name: Build issue body
        if: steps.compare.outputs.any_changed == 'true' && steps.existing.outputs.open_issues == '0'
        run: |
          upstream_sha=$(cat /tmp/upstream_sha.txt)
          upstream_sha_short=$(cat /tmp/upstream_sha_short.txt)

          # Build the issue body by concatenating safe static parts with file content.
          # Avoids unquoted heredocs to prevent shell expansion of untrusted diff content.
          # shellcheck disable=SC2016,SC2129
          printf '%s\n' 'The upstream [`stellar/stellar-xdr`](https://github.com/stellar/stellar-xdr) `curr` branch has changes that are not yet reflected in this SDK.' > /tmp/issue_body.md
          # shellcheck disable=SC2016,SC2129
          printf '\n**Upstream commit:** [`%s`](https://github.com/stellar/stellar-xdr/commit/%s)\n' "$upstream_sha_short" "$upstream_sha" >> /tmp/issue_body.md
          printf '\n## Changed files\n\n' >> /tmp/issue_body.md
          cat /tmp/changes_summary.txt >> /tmp/issue_body.md
          printf '\n## Diffs\n' >> /tmp/issue_body.md
          cat /tmp/full_diff.txt >> /tmp/issue_body.md
          printf '\n## Required actions\n\n' >> /tmp/issue_body.md
          # shellcheck disable=SC2016
          printf '%s\n' \
            '1. Update `XDR_COMMIT` in the repo-root `Makefile` to the upstream commit shown above' \
            '2. Run `make xdr-update` to re-download `.x` files and regenerate the Dart XDR types' \
            '3. Run `make xdr-generate-tests` to re-emit the XDR unit tests' \
            '4. Update `sdk_xdr_commit` in `tools/sep-51-oracle/oracle-pin.json` to the same commit. The SEP-0051 corpus generator refuses to run while that field disagrees with the `Makefile`' \
            '5. Install the pinned XDR-JSON reference CLI, whose exact command is recorded in `tools/sep-51-oracle/oracle-pin.json` under `install`' \
            '6. Run `ruby tools/sep-51-oracle/name_map.rb --diff` to rebuild the SEP-0051 name table and check every derived JSON name against the reference' \
            '7. Run `python3 tools/sep-51-corpus/generate_corpus.py` to regenerate the SEP-0051 conformance corpus against the same build' \
            '8. Run `make sep51-generate-tests` to re-emit the SEP-0051 tests and the embedded corpus' \
            '9. Run `dart analyze lib/` and fix any issues in the SDK layer (operation builders, TxRep, SEP implementations, etc.)' \
            '10. Run `flutter test` and fix any test failures' \
            '11. If the XDR changes introduce new types or extend existing ones (e.g. new operations, transaction fields, or protocol features), evaluate whether the SDK needs new or updated high-level APIs to support them (e.g. operation builders, TxRep, service classes)' \
            '12. Update the changelog and release notes with any breaking changes or new features. A changed JSON key or rendering is a wire-format change and belongs there too' >> /tmp/issue_body.md
          printf '\nSee the [XDR generator README](tools/xdr-generator/README.md) for detailed instructions.\n' >> /tmp/issue_body.md
          printf '\n---\n*This issue was created automatically by the [XDR update check workflow](.github/workflows/xdr-update-check.yml).*\n' >> /tmp/issue_body.md

      - name: Create issue
        if: steps.compare.outputs.any_changed == 'true' && steps.existing.outputs.open_issues == '0'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh issue create \
            --title "Update XDR definitions from upstream stellar-xdr" \
            --label "xdr-update" \
            --body-file /tmp/issue_body.md
