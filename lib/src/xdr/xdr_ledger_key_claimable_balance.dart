// Copyright 2026 The Stellar Flutter SDK Authors. All rights reserved.
// Use of this source code is governed by a license that can be
// found in the LICENSE file.
// This file is automatically generated by xdrgen. Do not edit manually.

import 'dart:convert';
import 'dart:typed_data';

import 'xdr_claimable_balance_id.dart';
import 'xdr_data_io.dart';
import 'xdr_json_helper.dart';

class XdrLedgerKeyClaimableBalance {
  XdrClaimableBalanceID _balanceID;
  XdrClaimableBalanceID get balanceID => this._balanceID;
  set balanceID(XdrClaimableBalanceID value) => this._balanceID = value;

  XdrLedgerKeyClaimableBalance(this._balanceID);

  static void encode(
    XdrDataOutputStream stream,
    XdrLedgerKeyClaimableBalance encodedLedgerKeyClaimableBalance,
  ) {
    XdrClaimableBalanceID.encode(
      stream,
      encodedLedgerKeyClaimableBalance.balanceID,
    );
  }

  static XdrLedgerKeyClaimableBalance decode(XdrDataInputStream stream) {
    XdrClaimableBalanceID balanceID = XdrClaimableBalanceID.decode(stream);
    return XdrLedgerKeyClaimableBalance(balanceID);
  }

  String toBase64EncodedXdrString() {
    XdrDataOutputStream xdrOutputStream = XdrDataOutputStream();
    XdrLedgerKeyClaimableBalance.encode(xdrOutputStream, this);
    return base64Encode(xdrOutputStream.bytes);
  }

  static XdrLedgerKeyClaimableBalance fromBase64EncodedXdrString(
    String base64Encoded,
  ) {
    Uint8List bytes = base64Decode(base64Encoded);
    return XdrLedgerKeyClaimableBalance.decode(XdrDataInputStream(bytes));
  }

  void toTxRep(String prefix, List<String> lines) {
    _balanceID.toTxRep('$prefix.balanceID', lines);
  }

  static XdrLedgerKeyClaimableBalance fromTxRep(
    Map<String, String> map,
    String prefix,
  ) {
    XdrClaimableBalanceID balanceID = XdrClaimableBalanceID.fromTxRep(
      map,
      '$prefix.balanceID',
    );
    return XdrLedgerKeyClaimableBalance(balanceID);
  }

  /// Returns the SEP-0051 XDR-JSON rendering of this value.
  String toXdrJson() => XdrJsonHelper.encodeDocument(
    toXdrJsonValue(),
    type: 'XdrLedgerKeyClaimableBalance',
  );

  /// Parses the SEP-0051 XDR-JSON rendering of a XdrLedgerKeyClaimableBalance.
  static XdrLedgerKeyClaimableBalance fromXdrJson(String json) =>
      fromXdrJsonValue(
        XdrJsonHelper.decodeDocument(
          json,
          type: 'XdrLedgerKeyClaimableBalance',
        ),
      );

  /// Returns the SEP-0051 rendering of this XdrLedgerKeyClaimableBalance.
  Object? toXdrJsonValue() => <String, Object?>{
    'balance_id': _balanceID.toXdrJsonValue(),
  };

  /// Reads a XdrLedgerKeyClaimableBalance from its SEP-0051 rendering.
  static XdrLedgerKeyClaimableBalance fromXdrJsonValue(Object? value) {
    final Map<String, dynamic> object = XdrJsonHelper.readObject(
      value,
      type: 'XdrLedgerKeyClaimableBalance',
      allowedKeys: const <String>{'balance_id'},
    );
    final Object? jsonBalanceID = XdrJsonHelper.readField(
      object,
      'balance_id',
      type: 'XdrLedgerKeyClaimableBalance',
    );
    return XdrLedgerKeyClaimableBalance(
      XdrClaimableBalanceID.fromXdrJsonValue(jsonBalanceID),
    );
  }
}
