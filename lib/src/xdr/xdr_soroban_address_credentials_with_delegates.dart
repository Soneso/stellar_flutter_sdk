// Copyright 2026 The Stellar Flutter SDK Authors. All rights reserved.
// Use of this source code is governed by a license that can be
// found in the LICENSE file.
// This file is automatically generated by xdrgen. Do not edit manually.

import 'dart:convert';
import 'dart:typed_data';

import 'txrep_helper.dart';
import 'xdr_data_io.dart';
import 'xdr_json_helper.dart';
import 'xdr_soroban_address_credentials.dart';
import 'xdr_soroban_delegate_signature.dart';

class XdrSorobanAddressCredentialsWithDelegates {
  XdrSorobanAddressCredentials _addressCredentials;
  XdrSorobanAddressCredentials get addressCredentials =>
      this._addressCredentials;
  set addressCredentials(XdrSorobanAddressCredentials value) =>
      this._addressCredentials = value;

  List<XdrSorobanDelegateSignature> _delegates;
  List<XdrSorobanDelegateSignature> get delegates => this._delegates;
  set delegates(List<XdrSorobanDelegateSignature> value) =>
      this._delegates = value;

  XdrSorobanAddressCredentialsWithDelegates(
    this._addressCredentials,
    this._delegates,
  );

  static void encode(
    XdrDataOutputStream stream,
    XdrSorobanAddressCredentialsWithDelegates
    encodedSorobanAddressCredentialsWithDelegates,
  ) {
    XdrSorobanAddressCredentials.encode(
      stream,
      encodedSorobanAddressCredentialsWithDelegates.addressCredentials,
    );
    int delegatessize =
        encodedSorobanAddressCredentialsWithDelegates.delegates.length;
    stream.writeInt(delegatessize);
    for (int i = 0; i < delegatessize; i++) {
      XdrSorobanDelegateSignature.encode(
        stream,
        encodedSorobanAddressCredentialsWithDelegates.delegates[i],
      );
    }
  }

  static XdrSorobanAddressCredentialsWithDelegates decode(
    XdrDataInputStream stream,
  ) {
    XdrSorobanAddressCredentials addressCredentials =
        XdrSorobanAddressCredentials.decode(stream);
    int delegatessize = stream.readInt();
    List<XdrSorobanDelegateSignature> delegates =
        List<XdrSorobanDelegateSignature>.empty(growable: true);
    for (int i = 0; i < delegatessize; i++) {
      delegates.add(XdrSorobanDelegateSignature.decode(stream));
    }
    return XdrSorobanAddressCredentialsWithDelegates(
      addressCredentials,
      delegates,
    );
  }

  String toBase64EncodedXdrString() {
    XdrDataOutputStream xdrOutputStream = XdrDataOutputStream();
    XdrSorobanAddressCredentialsWithDelegates.encode(xdrOutputStream, this);
    return base64Encode(xdrOutputStream.bytes);
  }

  static XdrSorobanAddressCredentialsWithDelegates fromBase64EncodedXdrString(
    String base64Encoded,
  ) {
    Uint8List bytes = base64Decode(base64Encoded);
    return XdrSorobanAddressCredentialsWithDelegates.decode(
      XdrDataInputStream(bytes),
    );
  }

  void toTxRep(String prefix, List<String> lines) {
    _addressCredentials.toTxRep('$prefix.addressCredentials', lines);
    lines.add('$prefix.delegates.len: ${_delegates.length}');
    for (int i = 0; i < _delegates.length; i++) {
      _delegates[i].toTxRep('$prefix.delegates[$i]', lines);
    }
  }

  static XdrSorobanAddressCredentialsWithDelegates fromTxRep(
    Map<String, String> map,
    String prefix,
  ) {
    XdrSorobanAddressCredentials addressCredentials =
        XdrSorobanAddressCredentials.fromTxRep(
          map,
          '$prefix.addressCredentials',
        );
    int delegatesLen = TxRepHelper.parseInt(
      TxRepHelper.getValue(map, '$prefix.delegates.len') ?? '0',
    );
    List<XdrSorobanDelegateSignature> delegates = [];
    for (int i = 0; i < delegatesLen; i++) {
      delegates.add(
        XdrSorobanDelegateSignature.fromTxRep(map, '$prefix.delegates[$i]'),
      );
    }
    return XdrSorobanAddressCredentialsWithDelegates(
      addressCredentials,
      delegates,
    );
  }

  /// Returns the SEP-0051 XDR-JSON rendering of this value.
  String toXdrJson() => XdrJsonHelper.encodeDocument(
    toXdrJsonValue(),
    type: 'XdrSorobanAddressCredentialsWithDelegates',
  );

  /// Parses the SEP-0051 XDR-JSON rendering of a XdrSorobanAddressCredentialsWithDelegates.
  static XdrSorobanAddressCredentialsWithDelegates fromXdrJson(String json) =>
      fromXdrJsonValue(
        XdrJsonHelper.decodeDocument(
          json,
          type: 'XdrSorobanAddressCredentialsWithDelegates',
        ),
      );

  /// Returns the SEP-0051 rendering of this XdrSorobanAddressCredentialsWithDelegates.
  Object? toXdrJsonValue() => <String, Object?>{
    'address_credentials': _addressCredentials.toXdrJsonValue(),
    'delegates': XdrJsonHelper.array<XdrSorobanDelegateSignature>(
      _delegates,
      (XdrSorobanDelegateSignature v) => v.toXdrJsonValue(),
      type: 'XdrSorobanAddressCredentialsWithDelegates',
      key: 'delegates',
    ),
  };

  /// Reads a XdrSorobanAddressCredentialsWithDelegates from its SEP-0051 rendering.
  static XdrSorobanAddressCredentialsWithDelegates fromXdrJsonValue(
    Object? value,
  ) {
    final Map<String, dynamic> object = XdrJsonHelper.readObject(
      value,
      type: 'XdrSorobanAddressCredentialsWithDelegates',
      allowedKeys: const <String>{'address_credentials', 'delegates'},
    );
    final Object? jsonAddressCredentials = XdrJsonHelper.readField(
      object,
      'address_credentials',
      type: 'XdrSorobanAddressCredentialsWithDelegates',
    );
    final Object? jsonDelegates = XdrJsonHelper.readField(
      object,
      'delegates',
      type: 'XdrSorobanAddressCredentialsWithDelegates',
    );
    return XdrSorobanAddressCredentialsWithDelegates(
      XdrSorobanAddressCredentials.fromXdrJsonValue(jsonAddressCredentials),
      XdrJsonHelper.readArray(
            jsonDelegates,
            type: 'XdrSorobanAddressCredentialsWithDelegates',
            key: 'delegates',
          )
          .map<XdrSorobanDelegateSignature>(
            (Object? e) => XdrSorobanDelegateSignature.fromXdrJsonValue(e),
          )
          .toList(),
    );
  }
}
