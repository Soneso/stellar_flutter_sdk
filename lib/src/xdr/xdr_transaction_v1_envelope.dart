// Copyright 2026 The Stellar Flutter SDK Authors. All rights reserved.
// Use of this source code is governed by a license that can be
// found in the LICENSE file.
// This file is automatically generated by xdrgen. Do not edit manually.

import 'dart:convert';
import 'dart:typed_data';

import 'txrep_helper.dart';
import 'xdr_data_io.dart';
import 'xdr_decorated_signature.dart';
import 'xdr_json_helper.dart';
import 'xdr_transaction.dart';

class XdrTransactionV1Envelope {
  XdrTransaction _tx;
  XdrTransaction get tx => this._tx;
  set tx(XdrTransaction value) => this._tx = value;

  List<XdrDecoratedSignature> _signatures;
  List<XdrDecoratedSignature> get signatures => this._signatures;
  set signatures(List<XdrDecoratedSignature> value) => this._signatures = value;

  XdrTransactionV1Envelope(this._tx, this._signatures);

  static void encode(
    XdrDataOutputStream stream,
    XdrTransactionV1Envelope encodedTransactionV1Envelope,
  ) {
    XdrTransaction.encode(stream, encodedTransactionV1Envelope.tx);
    int signaturessize = encodedTransactionV1Envelope.signatures.length;
    stream.writeInt(signaturessize);
    for (int i = 0; i < signaturessize; i++) {
      XdrDecoratedSignature.encode(
        stream,
        encodedTransactionV1Envelope.signatures[i],
      );
    }
  }

  static XdrTransactionV1Envelope decode(XdrDataInputStream stream) {
    XdrTransaction tx = XdrTransaction.decode(stream);
    int signaturessize = stream.readInt();
    List<XdrDecoratedSignature> signatures = List<XdrDecoratedSignature>.empty(
      growable: true,
    );
    for (int i = 0; i < signaturessize; i++) {
      signatures.add(XdrDecoratedSignature.decode(stream));
    }
    return XdrTransactionV1Envelope(tx, signatures);
  }

  String toBase64EncodedXdrString() {
    XdrDataOutputStream xdrOutputStream = XdrDataOutputStream();
    XdrTransactionV1Envelope.encode(xdrOutputStream, this);
    return base64Encode(xdrOutputStream.bytes);
  }

  static XdrTransactionV1Envelope fromBase64EncodedXdrString(
    String base64Encoded,
  ) {
    Uint8List bytes = base64Decode(base64Encoded);
    return XdrTransactionV1Envelope.decode(XdrDataInputStream(bytes));
  }

  void toTxRep(String prefix, List<String> lines) {
    _tx.toTxRep('$prefix.tx', lines);
    lines.add('$prefix.signatures.len: ${_signatures.length}');
    for (int i = 0; i < _signatures.length; i++) {
      _signatures[i].toTxRep('$prefix.signatures[$i]', lines);
    }
  }

  static XdrTransactionV1Envelope fromTxRep(
    Map<String, String> map,
    String prefix,
  ) {
    XdrTransaction tx = XdrTransaction.fromTxRep(map, '$prefix.tx');
    int signaturesLen = TxRepHelper.parseInt(
      TxRepHelper.getValue(map, '$prefix.signatures.len') ?? '0',
    );
    List<XdrDecoratedSignature> signatures = [];
    for (int i = 0; i < signaturesLen; i++) {
      signatures.add(
        XdrDecoratedSignature.fromTxRep(map, '$prefix.signatures[$i]'),
      );
    }
    return XdrTransactionV1Envelope(tx, signatures);
  }

  /// Returns the SEP-0051 XDR-JSON rendering of this value.
  String toXdrJson() => XdrJsonHelper.encodeDocument(
    toXdrJsonValue(),
    type: 'XdrTransactionV1Envelope',
  );

  /// Parses the SEP-0051 XDR-JSON rendering of a XdrTransactionV1Envelope.
  static XdrTransactionV1Envelope fromXdrJson(String json) => fromXdrJsonValue(
    XdrJsonHelper.decodeDocument(json, type: 'XdrTransactionV1Envelope'),
  );

  /// Returns the SEP-0051 rendering of this XdrTransactionV1Envelope.
  Object? toXdrJsonValue() => <String, Object?>{
    'tx': _tx.toXdrJsonValue(),
    'signatures': XdrJsonHelper.array<XdrDecoratedSignature>(
      _signatures,
      (XdrDecoratedSignature v) => v.toXdrJsonValue(),
      type: 'XdrTransactionV1Envelope',
      key: 'signatures',
      maxLength: 20,
    ),
  };

  /// Reads a XdrTransactionV1Envelope from its SEP-0051 rendering.
  static XdrTransactionV1Envelope fromXdrJsonValue(Object? value) {
    final Map<String, dynamic> object = XdrJsonHelper.readObject(
      value,
      type: 'XdrTransactionV1Envelope',
      allowedKeys: const <String>{'tx', 'signatures'},
    );
    final Object? jsonTx = XdrJsonHelper.readField(
      object,
      'tx',
      type: 'XdrTransactionV1Envelope',
    );
    final Object? jsonSignatures = XdrJsonHelper.readField(
      object,
      'signatures',
      type: 'XdrTransactionV1Envelope',
    );
    return XdrTransactionV1Envelope(
      XdrTransaction.fromXdrJsonValue(jsonTx),
      XdrJsonHelper.readArray(
            jsonSignatures,
            type: 'XdrTransactionV1Envelope',
            key: 'signatures',
            maxLength: 20,
          )
          .map<XdrDecoratedSignature>(
            (Object? e) => XdrDecoratedSignature.fromXdrJsonValue(e),
          )
          .toList(),
    );
  }
}
