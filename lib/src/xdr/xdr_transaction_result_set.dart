// Copyright 2026 The Stellar Flutter SDK Authors. All rights reserved.
// Use of this source code is governed by a license that can be
// found in the LICENSE file.
// This file is automatically generated by xdrgen. Do not edit manually.

import 'dart:convert';
import 'dart:typed_data';

import 'xdr_data_io.dart';
import 'xdr_json_helper.dart';
import 'xdr_transaction_result_pair.dart';

class XdrTransactionResultSet {
  List<XdrTransactionResultPair> _results;
  List<XdrTransactionResultPair> get results => this._results;
  set results(List<XdrTransactionResultPair> value) => this._results = value;

  XdrTransactionResultSet(this._results);

  static void encode(
    XdrDataOutputStream stream,
    XdrTransactionResultSet encodedTransactionResultSet,
  ) {
    int resultssize = encodedTransactionResultSet.results.length;
    stream.writeInt(resultssize);
    for (int i = 0; i < resultssize; i++) {
      XdrTransactionResultPair.encode(
        stream,
        encodedTransactionResultSet.results[i],
      );
    }
  }

  static XdrTransactionResultSet decode(XdrDataInputStream stream) {
    int resultssize = stream.readInt();
    List<XdrTransactionResultPair> results =
        List<XdrTransactionResultPair>.empty(growable: true);
    for (int i = 0; i < resultssize; i++) {
      results.add(XdrTransactionResultPair.decode(stream));
    }
    return XdrTransactionResultSet(results);
  }

  String toBase64EncodedXdrString() {
    XdrDataOutputStream xdrOutputStream = XdrDataOutputStream();
    XdrTransactionResultSet.encode(xdrOutputStream, this);
    return base64Encode(xdrOutputStream.bytes);
  }

  static XdrTransactionResultSet fromBase64EncodedXdrString(
    String base64Encoded,
  ) {
    Uint8List bytes = base64Decode(base64Encoded);
    return XdrTransactionResultSet.decode(XdrDataInputStream(bytes));
  }

  /// Returns the SEP-0051 XDR-JSON rendering of this value.
  String toXdrJson() => XdrJsonHelper.encodeDocument(
    toXdrJsonValue(),
    type: 'XdrTransactionResultSet',
  );

  /// Parses the SEP-0051 XDR-JSON rendering of a XdrTransactionResultSet.
  static XdrTransactionResultSet fromXdrJson(String json) => fromXdrJsonValue(
    XdrJsonHelper.decodeDocument(json, type: 'XdrTransactionResultSet'),
  );

  /// Returns the SEP-0051 rendering of this XdrTransactionResultSet.
  Object? toXdrJsonValue() => <String, Object?>{
    'results': XdrJsonHelper.array<XdrTransactionResultPair>(
      _results,
      (XdrTransactionResultPair v) => v.toXdrJsonValue(),
      type: 'XdrTransactionResultSet',
      key: 'results',
    ),
  };

  /// Reads a XdrTransactionResultSet from its SEP-0051 rendering.
  static XdrTransactionResultSet fromXdrJsonValue(Object? value) {
    final Map<String, dynamic> object = XdrJsonHelper.readObject(
      value,
      type: 'XdrTransactionResultSet',
      allowedKeys: const <String>{'results'},
    );
    final Object? jsonResults = XdrJsonHelper.readField(
      object,
      'results',
      type: 'XdrTransactionResultSet',
    );
    return XdrTransactionResultSet(
      XdrJsonHelper.readArray(
            jsonResults,
            type: 'XdrTransactionResultSet',
            key: 'results',
          )
          .map<XdrTransactionResultPair>(
            (Object? e) => XdrTransactionResultPair.fromXdrJsonValue(e),
          )
          .toList(),
    );
  }
}
