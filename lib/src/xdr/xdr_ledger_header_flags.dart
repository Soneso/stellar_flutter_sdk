// Copyright 2026 The Stellar Flutter SDK Authors. All rights reserved.
// Use of this source code is governed by a license that can be
// found in the LICENSE file.
// This file is automatically generated by xdrgen. Do not edit manually.

import 'dart:convert';
import 'dart:typed_data';

import 'xdr_data_io.dart';
import 'xdr_json_helper.dart';

class XdrLedgerHeaderFlags {
  final _value;
  const XdrLedgerHeaderFlags._internal(this._value);
  toString() => 'LedgerHeaderFlags.$_value';
  XdrLedgerHeaderFlags(this._value);
  get value => this._value;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is XdrLedgerHeaderFlags && _value == other._value;

  @override
  int get hashCode => _value.hashCode;

  static const DISABLE_LIQUIDITY_POOL_TRADING_FLAG =
      const XdrLedgerHeaderFlags._internal(1);
  static const DISABLE_LIQUIDITY_POOL_DEPOSIT_FLAG =
      const XdrLedgerHeaderFlags._internal(2);
  static const DISABLE_LIQUIDITY_POOL_WITHDRAWAL_FLAG =
      const XdrLedgerHeaderFlags._internal(4);

  static XdrLedgerHeaderFlags decode(XdrDataInputStream stream) {
    int value = stream.readInt();
    switch (value) {
      case 1:
        return DISABLE_LIQUIDITY_POOL_TRADING_FLAG;
      case 2:
        return DISABLE_LIQUIDITY_POOL_DEPOSIT_FLAG;
      case 4:
        return DISABLE_LIQUIDITY_POOL_WITHDRAWAL_FLAG;
      default:
        throw Exception("Unknown enum value: $value");
    }
  }

  static void encode(XdrDataOutputStream stream, XdrLedgerHeaderFlags value) {
    stream.writeInt(value.value);
  }

  String toBase64EncodedXdrString() {
    XdrDataOutputStream xdrOutputStream = XdrDataOutputStream();
    XdrLedgerHeaderFlags.encode(xdrOutputStream, this);
    return base64Encode(xdrOutputStream.bytes);
  }

  static XdrLedgerHeaderFlags fromBase64EncodedXdrString(String base64Encoded) {
    Uint8List bytes = base64Decode(base64Encoded);
    return XdrLedgerHeaderFlags.decode(XdrDataInputStream(bytes));
  }

  /// Returns the SEP-0051 XDR-JSON rendering of this value.
  String toXdrJson() => XdrJsonHelper.encodeDocument(
    toXdrJsonValue(),
    type: 'XdrLedgerHeaderFlags',
  );

  /// Parses the SEP-0051 XDR-JSON rendering of a XdrLedgerHeaderFlags.
  static XdrLedgerHeaderFlags fromXdrJson(String json) => fromXdrJsonValue(
    XdrJsonHelper.decodeDocument(json, type: 'XdrLedgerHeaderFlags'),
  );

  /// Returns this member's SEP-0051 name.
  Object? toXdrJsonValue() {
    switch (_value) {
      case 1:
        return 'trading_flag';
      case 2:
        return 'deposit_flag';
      case 4:
        return 'withdrawal_flag';
      default:
        XdrJsonHelper.fail(
          'XdrLedgerHeaderFlags',
          'holds the unknown value $_value',
        );
    }
  }

  /// Reads a XdrLedgerHeaderFlags from its SEP-0051 name.
  static XdrLedgerHeaderFlags fromXdrJsonValue(Object? value) {
    if (value is String) {
      switch (value) {
        case 'trading_flag':
          return XdrLedgerHeaderFlags.DISABLE_LIQUIDITY_POOL_TRADING_FLAG;
        case 'deposit_flag':
          return XdrLedgerHeaderFlags.DISABLE_LIQUIDITY_POOL_DEPOSIT_FLAG;
        case 'withdrawal_flag':
          return XdrLedgerHeaderFlags.DISABLE_LIQUIDITY_POOL_WITHDRAWAL_FLAG;
      }
    }
    XdrJsonHelper.fail(
      'XdrLedgerHeaderFlags',
      'expects one of its member names but found ${XdrJsonHelper.preview(value)}',
    );
  }
}
