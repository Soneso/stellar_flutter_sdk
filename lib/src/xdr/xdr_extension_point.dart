// Copyright 2026 The Stellar Flutter SDK Authors. All rights reserved.
// Use of this source code is governed by a license that can be
// found in the LICENSE file.
// This file is automatically generated by xdrgen. Do not edit manually.

import 'dart:convert';
import 'dart:typed_data';

import 'txrep_helper.dart';
import 'xdr_data_io.dart';

class XdrExtensionPoint {
  int _v;

  int get discriminant => this._v;
  set discriminant(int value) => this._v = value;

  /// Alias for [discriminant], the original XDR field name.
  int get v => this._v;
  set v(int value) => this._v = value;

  XdrExtensionPoint(this._v);

  static void encode(XdrDataOutputStream stream, XdrExtensionPoint encodedExtensionPoint) {
    stream.writeInt(encodedExtensionPoint.discriminant);
    switch (encodedExtensionPoint.discriminant) {
      case 0:
        break;
      default:
        break;
    }
  }

  static XdrExtensionPoint decode(XdrDataInputStream stream) {
    int discriminant = stream.readInt();
    XdrExtensionPoint decodedExtensionPoint = XdrExtensionPoint(discriminant);
    switch (decodedExtensionPoint.discriminant) {
      case 0:
        break;
      default:
        break;
    }
    return decodedExtensionPoint;
  }

  String toBase64EncodedXdrString() {
    XdrDataOutputStream xdrOutputStream = XdrDataOutputStream();
    XdrExtensionPoint.encode(xdrOutputStream, this);
    return base64Encode(xdrOutputStream.bytes);
  }

  static XdrExtensionPoint fromBase64EncodedXdrString(String base64Encoded) {
    Uint8List bytes = base64Decode(base64Encoded);
    return XdrExtensionPoint.decode(XdrDataInputStream(bytes));
  }

  void toTxRep(String prefix, List<String> lines) {
    lines.add('$prefix.v: $discriminant');
    switch (discriminant) {
      case 0:
        break;
      default:
        break;
    }
  }

  static XdrExtensionPoint fromTxRep(Map<String, String> map, String prefix) {
    int disc = TxRepHelper.parseInt(TxRepHelper.getValue(map, '$prefix.v') ?? '0');
    XdrExtensionPoint result = XdrExtensionPoint(disc);
    switch (result.discriminant) {
      case 0:
        break;
      default:
        break;
    }
    return result;
  }
}
