// Copyright 2026 The Stellar Flutter SDK Authors. All rights reserved.
// Use of this source code is governed by a license that can be
// found in the LICENSE file.
// This file is automatically generated by xdrgen. Do not edit manually.

import 'dart:convert';
import 'dart:typed_data';

import 'txrep_helper.dart';
import 'xdr_data_io.dart';
import 'xdr_json_helper.dart';

class XdrHash {
  XdrHash(this._hash);

  Uint8List _hash;
  Uint8List get hash => this._hash;
  set hash(Uint8List value) => this._hash = value;

  static void encode(XdrDataOutputStream stream, XdrHash encodedHash) {
    stream.write(encodedHash.hash);
  }

  static XdrHash decode(XdrDataInputStream stream) {
    int hashSize = 32;
    return XdrHash(stream.readBytes(hashSize));
  }

  String toBase64EncodedXdrString() {
    XdrDataOutputStream xdrOutputStream = XdrDataOutputStream();
    XdrHash.encode(xdrOutputStream, this);
    return base64Encode(xdrOutputStream.bytes);
  }

  static XdrHash fromBase64EncodedXdrString(String base64Encoded) {
    Uint8List bytes = base64Decode(base64Encoded);
    return XdrHash.decode(XdrDataInputStream(bytes));
  }

  void toTxRep(String prefix, List<String> lines) {
    lines.add('$prefix: ${TxRepHelper.bytesToHex(_hash)}');
  }

  static XdrHash fromTxRep(Map<String, String> map, String prefix) {
    String? raw = TxRepHelper.getValue(map, prefix);
    if (raw == null) throw Exception('missing $prefix');
    return XdrHash(TxRepHelper.hexToBytes(raw));
  }

  /// Returns the SEP-0051 XDR-JSON rendering of this value.
  String toXdrJson() =>
      XdrJsonHelper.encodeDocument(toXdrJsonValue(), type: 'XdrHash');

  /// Parses the SEP-0051 XDR-JSON rendering of a XdrHash.
  static XdrHash fromXdrJson(String json) =>
      fromXdrJsonValue(XdrJsonHelper.decodeDocument(json, type: 'XdrHash'));

  /// Returns the SEP-0051 rendering of the wrapped value.
  Object? toXdrJsonValue() => XdrJsonHelper.hex(_hash, type: 'XdrHash');

  /// Reads a XdrHash from the SEP-0051 rendering of its value.
  static XdrHash fromXdrJsonValue(Object? value) => XdrHash(
    XdrJsonHelper.readHex(value, type: 'XdrHash', expectedLength: 32),
  );
}
