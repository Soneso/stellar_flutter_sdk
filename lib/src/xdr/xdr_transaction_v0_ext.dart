// Copyright 2026 The Stellar Flutter SDK Authors. All rights reserved.
// Use of this source code is governed by a license that can be
// found in the LICENSE file.
// This file is automatically generated by xdrgen. Do not edit manually.

import 'dart:convert';
import 'dart:typed_data';

import 'txrep_helper.dart';
import 'xdr_data_io.dart';
import 'xdr_json_helper.dart';

class XdrTransactionV0Ext {
  int _v;

  int get discriminant => this._v;
  set discriminant(int value) => this._v = value;

  /// Alias for [discriminant], the original XDR field name.
  int get v => this._v;
  set v(int value) => this._v = value;

  XdrTransactionV0Ext(this._v);

  static void encode(
    XdrDataOutputStream stream,
    XdrTransactionV0Ext encodedTransactionV0Ext,
  ) {
    stream.writeInt(encodedTransactionV0Ext.discriminant);
    switch (encodedTransactionV0Ext.discriminant) {
      case 0:
        break;
      default:
        break;
    }
  }

  static XdrTransactionV0Ext decode(XdrDataInputStream stream) {
    int discriminant = stream.readInt();
    XdrTransactionV0Ext decodedTransactionV0Ext = XdrTransactionV0Ext(
      discriminant,
    );
    switch (decodedTransactionV0Ext.discriminant) {
      case 0:
        break;
      default:
        break;
    }
    return decodedTransactionV0Ext;
  }

  String toBase64EncodedXdrString() {
    XdrDataOutputStream xdrOutputStream = XdrDataOutputStream();
    XdrTransactionV0Ext.encode(xdrOutputStream, this);
    return base64Encode(xdrOutputStream.bytes);
  }

  static XdrTransactionV0Ext fromBase64EncodedXdrString(String base64Encoded) {
    Uint8List bytes = base64Decode(base64Encoded);
    return XdrTransactionV0Ext.decode(XdrDataInputStream(bytes));
  }

  void toTxRep(String prefix, List<String> lines) {
    lines.add('$prefix.v: $discriminant');
    switch (discriminant) {
      case 0:
        break;
      default:
        break;
    }
  }

  static XdrTransactionV0Ext fromTxRep(Map<String, String> map, String prefix) {
    int disc = TxRepHelper.parseInt(
      TxRepHelper.getValue(map, '$prefix.v') ?? '0',
    );
    XdrTransactionV0Ext result = XdrTransactionV0Ext(disc);
    switch (result.discriminant) {
      case 0:
        break;
      default:
        break;
    }
    return result;
  }

  /// Returns the SEP-0051 XDR-JSON rendering of this value.
  String toXdrJson() => XdrJsonHelper.encodeDocument(
    toXdrJsonValue(),
    type: 'XdrTransactionV0Ext',
  );

  /// Parses the SEP-0051 XDR-JSON rendering of a XdrTransactionV0Ext.
  static XdrTransactionV0Ext fromXdrJson(String json) => fromXdrJsonValue(
    XdrJsonHelper.decodeDocument(json, type: 'XdrTransactionV0Ext'),
  );

  /// Returns the SEP-0051 rendering of this XdrTransactionV0Ext.
  Object? toXdrJsonValue() {
    switch (discriminant) {
      case 0:
        return 'v0';
    }
    XdrJsonHelper.fail(
      'XdrTransactionV0Ext',
      'holds the unknown discriminant ${discriminant}',
    );
  }

  /// Reads a XdrTransactionV0Ext from its SEP-0051 rendering.
  static XdrTransactionV0Ext fromXdrJsonValue(Object? value) {
    if (value is String) {
      switch (value) {
        case 'v0':
          return XdrTransactionV0Ext(0);
      }
      XdrJsonHelper.fail(
        'XdrTransactionV0Ext',
        'has no arm named ${XdrJsonHelper.preview(value)}',
      );
    }
    XdrJsonHelper.fail(
      'XdrTransactionV0Ext',
      'expects one of its arm names but found ${XdrJsonHelper.preview(value)}',
    );
  }
}
