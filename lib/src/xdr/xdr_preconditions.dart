// Copyright 2026 The Stellar Flutter SDK Authors. All rights reserved.
// Use of this source code is governed by a license that can be
// found in the LICENSE file.
// This file is automatically generated by xdrgen. Do not edit manually.

import 'dart:convert';
import 'dart:typed_data';

import 'xdr_data_io.dart';
import 'xdr_precondition_type.dart';
import 'xdr_preconditions_v2.dart';
import 'xdr_time_bounds.dart';

class XdrPreconditions {
  XdrPreconditionType _type;

  XdrPreconditionType get discriminant => this._type;

  set discriminant(XdrPreconditionType value) => this._type = value;

  XdrTimeBounds? _timeBounds;

  XdrTimeBounds? get timeBounds => this._timeBounds;

  XdrPreconditionsV2? _v2;

  XdrPreconditionsV2? get v2 => this._v2;

  XdrPreconditions(this._type);

  set timeBounds(XdrTimeBounds? value) => this._timeBounds = value;

  set v2(XdrPreconditionsV2? value) => this._v2 = value;

  static void encode(
    XdrDataOutputStream stream,
    XdrPreconditions encodedPreconditions,
  ) {
    stream.writeInt(encodedPreconditions.discriminant.value);
    switch (encodedPreconditions.discriminant) {
      case XdrPreconditionType.PRECOND_NONE:
        break;
      case XdrPreconditionType.PRECOND_TIME:
        XdrTimeBounds.encode(stream, encodedPreconditions._timeBounds!);
        break;
      case XdrPreconditionType.PRECOND_V2:
        XdrPreconditionsV2.encode(stream, encodedPreconditions._v2!);
        break;
      default:
        break;
    }
  }

  static XdrPreconditions decode(XdrDataInputStream stream) {
    XdrPreconditions decodedPreconditions = XdrPreconditions(
      XdrPreconditionType.decode(stream),
    );
    switch (decodedPreconditions.discriminant) {
      case XdrPreconditionType.PRECOND_NONE:
        break;
      case XdrPreconditionType.PRECOND_TIME:
        decodedPreconditions._timeBounds = XdrTimeBounds.decode(stream);
        break;
      case XdrPreconditionType.PRECOND_V2:
        decodedPreconditions._v2 = XdrPreconditionsV2.decode(stream);
        break;
      default:
        break;
    }
    return decodedPreconditions;
  }

  String toBase64EncodedXdrString() {
    XdrDataOutputStream xdrOutputStream = XdrDataOutputStream();
    XdrPreconditions.encode(xdrOutputStream, this);
    return base64Encode(xdrOutputStream.bytes);
  }

  static XdrPreconditions fromBase64EncodedXdrString(String base64Encoded) {
    Uint8List bytes = base64Decode(base64Encoded);
    return XdrPreconditions.decode(XdrDataInputStream(bytes));
  }
}
