// Copyright 2026 The Stellar Flutter SDK Authors. All rights reserved.
// Use of this source code is governed by a license that can be
// found in the LICENSE file.
// This file is automatically generated by xdrgen. Do not edit manually.

import 'dart:convert';
import 'dart:typed_data';

import 'xdr_data_io.dart';
import 'xdr_hash.dart';
import 'xdr_json_helper.dart';
import 'xdr_value.dart';

class XdrSCPNomination {
  XdrHash _quorumSetHash;
  XdrHash get quorumSetHash => this._quorumSetHash;
  set quorumSetHash(XdrHash value) => this._quorumSetHash = value;

  List<XdrValue> _votes;
  List<XdrValue> get votes => this._votes;
  set votes(List<XdrValue> value) => this._votes = value;

  List<XdrValue> _accepted;
  List<XdrValue> get accepted => this._accepted;
  set accepted(List<XdrValue> value) => this._accepted = value;

  XdrSCPNomination(this._quorumSetHash, this._votes, this._accepted);

  static void encode(
    XdrDataOutputStream stream,
    XdrSCPNomination encodedSCPNomination,
  ) {
    XdrHash.encode(stream, encodedSCPNomination.quorumSetHash);
    int votessize = encodedSCPNomination.votes.length;
    stream.writeInt(votessize);
    for (int i = 0; i < votessize; i++) {
      XdrValue.encode(stream, encodedSCPNomination.votes[i]);
    }
    int acceptedsize = encodedSCPNomination.accepted.length;
    stream.writeInt(acceptedsize);
    for (int i = 0; i < acceptedsize; i++) {
      XdrValue.encode(stream, encodedSCPNomination.accepted[i]);
    }
  }

  static XdrSCPNomination decode(XdrDataInputStream stream) {
    XdrHash quorumSetHash = XdrHash.decode(stream);
    int votessize = stream.readInt();
    List<XdrValue> votes = List<XdrValue>.empty(growable: true);
    for (int i = 0; i < votessize; i++) {
      votes.add(XdrValue.decode(stream));
    }
    int acceptedsize = stream.readInt();
    List<XdrValue> accepted = List<XdrValue>.empty(growable: true);
    for (int i = 0; i < acceptedsize; i++) {
      accepted.add(XdrValue.decode(stream));
    }
    return XdrSCPNomination(quorumSetHash, votes, accepted);
  }

  String toBase64EncodedXdrString() {
    XdrDataOutputStream xdrOutputStream = XdrDataOutputStream();
    XdrSCPNomination.encode(xdrOutputStream, this);
    return base64Encode(xdrOutputStream.bytes);
  }

  static XdrSCPNomination fromBase64EncodedXdrString(String base64Encoded) {
    Uint8List bytes = base64Decode(base64Encoded);
    return XdrSCPNomination.decode(XdrDataInputStream(bytes));
  }

  /// Returns the SEP-0051 XDR-JSON rendering of this value.
  String toXdrJson() =>
      XdrJsonHelper.encodeDocument(toXdrJsonValue(), type: 'XdrSCPNomination');

  /// Parses the SEP-0051 XDR-JSON rendering of a XdrSCPNomination.
  static XdrSCPNomination fromXdrJson(String json) => fromXdrJsonValue(
    XdrJsonHelper.decodeDocument(json, type: 'XdrSCPNomination'),
  );

  /// Returns the SEP-0051 rendering of this XdrSCPNomination.
  Object? toXdrJsonValue() => <String, Object?>{
    'quorum_set_hash': _quorumSetHash.toXdrJsonValue(),
    'votes': XdrJsonHelper.array<XdrValue>(
      _votes,
      (XdrValue v) => v.toXdrJsonValue(),
      type: 'XdrSCPNomination',
      key: 'votes',
    ),
    'accepted': XdrJsonHelper.array<XdrValue>(
      _accepted,
      (XdrValue v) => v.toXdrJsonValue(),
      type: 'XdrSCPNomination',
      key: 'accepted',
    ),
  };

  /// Reads a XdrSCPNomination from its SEP-0051 rendering.
  static XdrSCPNomination fromXdrJsonValue(Object? value) {
    final Map<String, dynamic> object = XdrJsonHelper.readObject(
      value,
      type: 'XdrSCPNomination',
      allowedKeys: const <String>{'quorum_set_hash', 'votes', 'accepted'},
    );
    final Object? jsonQuorumSetHash = XdrJsonHelper.readField(
      object,
      'quorum_set_hash',
      type: 'XdrSCPNomination',
    );
    final Object? jsonVotes = XdrJsonHelper.readField(
      object,
      'votes',
      type: 'XdrSCPNomination',
    );
    final Object? jsonAccepted = XdrJsonHelper.readField(
      object,
      'accepted',
      type: 'XdrSCPNomination',
    );
    return XdrSCPNomination(
      XdrHash.fromXdrJsonValue(jsonQuorumSetHash),
      XdrJsonHelper.readArray(
        jsonVotes,
        type: 'XdrSCPNomination',
        key: 'votes',
      ).map<XdrValue>((Object? e) => XdrValue.fromXdrJsonValue(e)).toList(),
      XdrJsonHelper.readArray(
        jsonAccepted,
        type: 'XdrSCPNomination',
        key: 'accepted',
      ).map<XdrValue>((Object? e) => XdrValue.fromXdrJsonValue(e)).toList(),
    );
  }
}
