// Copyright 2026 The Stellar Flutter SDK Authors. All rights reserved.
// Use of this source code is governed by a license that can be
// found in the LICENSE file.
// This file is automatically generated by xdrgen. Do not edit manually.

import 'dart:convert';
import 'dart:typed_data';

import 'txrep_helper.dart';
import 'xdr_data_io.dart';
import 'xdr_json_helper.dart';
import 'xdr_sc_address.dart';
import 'xdr_sc_val.dart';

class XdrSorobanDelegateSignature {
  XdrSCAddress _address;
  XdrSCAddress get address => this._address;
  set address(XdrSCAddress value) => this._address = value;

  XdrSCVal _signature;
  XdrSCVal get signature => this._signature;
  set signature(XdrSCVal value) => this._signature = value;

  List<XdrSorobanDelegateSignature> _nestedDelegates;
  List<XdrSorobanDelegateSignature> get nestedDelegates =>
      this._nestedDelegates;
  set nestedDelegates(List<XdrSorobanDelegateSignature> value) =>
      this._nestedDelegates = value;

  XdrSorobanDelegateSignature(
    this._address,
    this._signature,
    this._nestedDelegates,
  );

  static void encode(
    XdrDataOutputStream stream,
    XdrSorobanDelegateSignature encodedSorobanDelegateSignature,
  ) {
    XdrSCAddress.encode(stream, encodedSorobanDelegateSignature.address);
    XdrSCVal.encode(stream, encodedSorobanDelegateSignature.signature);
    int nestedDelegatessize =
        encodedSorobanDelegateSignature.nestedDelegates.length;
    stream.writeInt(nestedDelegatessize);
    for (int i = 0; i < nestedDelegatessize; i++) {
      XdrSorobanDelegateSignature.encode(
        stream,
        encodedSorobanDelegateSignature.nestedDelegates[i],
      );
    }
  }

  static XdrSorobanDelegateSignature decode(XdrDataInputStream stream) {
    stream.enterRecursiveDecode();
    try {
      XdrSCAddress address = XdrSCAddress.decode(stream);
      XdrSCVal signature = XdrSCVal.decode(stream);
      int nestedDelegatessize = stream.readInt();
      List<XdrSorobanDelegateSignature> nestedDelegates =
          List<XdrSorobanDelegateSignature>.empty(growable: true);
      for (int i = 0; i < nestedDelegatessize; i++) {
        nestedDelegates.add(XdrSorobanDelegateSignature.decode(stream));
      }
      return XdrSorobanDelegateSignature(address, signature, nestedDelegates);
    } finally {
      stream.exitRecursiveDecode();
    }
  }

  String toBase64EncodedXdrString() {
    XdrDataOutputStream xdrOutputStream = XdrDataOutputStream();
    XdrSorobanDelegateSignature.encode(xdrOutputStream, this);
    return base64Encode(xdrOutputStream.bytes);
  }

  static XdrSorobanDelegateSignature fromBase64EncodedXdrString(
    String base64Encoded,
  ) {
    Uint8List bytes = base64Decode(base64Encoded);
    return XdrSorobanDelegateSignature.decode(XdrDataInputStream(bytes));
  }

  void toTxRep(String prefix, List<String> lines) {
    _address.toTxRep('$prefix.address', lines);
    _signature.toTxRep('$prefix.signature', lines);
    lines.add('$prefix.nestedDelegates.len: ${_nestedDelegates.length}');
    for (int i = 0; i < _nestedDelegates.length; i++) {
      _nestedDelegates[i].toTxRep('$prefix.nestedDelegates[$i]', lines);
    }
  }

  static XdrSorobanDelegateSignature fromTxRep(
    Map<String, String> map,
    String prefix,
  ) {
    XdrSCAddress address = XdrSCAddress.fromTxRep(map, '$prefix.address');
    XdrSCVal signature = XdrSCVal.fromTxRep(map, '$prefix.signature');
    int nestedDelegatesLen = TxRepHelper.parseInt(
      TxRepHelper.getValue(map, '$prefix.nestedDelegates.len') ?? '0',
    );
    List<XdrSorobanDelegateSignature> nestedDelegates = [];
    for (int i = 0; i < nestedDelegatesLen; i++) {
      nestedDelegates.add(
        XdrSorobanDelegateSignature.fromTxRep(
          map,
          '$prefix.nestedDelegates[$i]',
        ),
      );
    }
    return XdrSorobanDelegateSignature(address, signature, nestedDelegates);
  }

  /// Returns the SEP-0051 XDR-JSON rendering of this value.
  String toXdrJson() => XdrJsonHelper.encodeDocument(
    toXdrJsonValue(),
    type: 'XdrSorobanDelegateSignature',
  );

  /// Parses the SEP-0051 XDR-JSON rendering of a XdrSorobanDelegateSignature.
  static XdrSorobanDelegateSignature fromXdrJson(String json) =>
      fromXdrJsonValue(
        XdrJsonHelper.decodeDocument(json, type: 'XdrSorobanDelegateSignature'),
      );

  /// Returns the SEP-0051 rendering of this XdrSorobanDelegateSignature.
  Object? toXdrJsonValue() => <String, Object?>{
    'address': _address.toXdrJsonValue(),
    'signature': _signature.toXdrJsonValue(),
    'nested_delegates': XdrJsonHelper.array<XdrSorobanDelegateSignature>(
      _nestedDelegates,
      (XdrSorobanDelegateSignature v) => v.toXdrJsonValue(),
      type: 'XdrSorobanDelegateSignature',
      key: 'nested_delegates',
    ),
  };

  /// Reads a XdrSorobanDelegateSignature from its SEP-0051 rendering.
  static XdrSorobanDelegateSignature fromXdrJsonValue(Object? value) {
    final Map<String, dynamic> object = XdrJsonHelper.readObject(
      value,
      type: 'XdrSorobanDelegateSignature',
      allowedKeys: const <String>{'address', 'signature', 'nested_delegates'},
    );
    final Object? jsonAddress = XdrJsonHelper.readField(
      object,
      'address',
      type: 'XdrSorobanDelegateSignature',
    );
    final Object? jsonSignature = XdrJsonHelper.readField(
      object,
      'signature',
      type: 'XdrSorobanDelegateSignature',
    );
    final Object? jsonNestedDelegates = XdrJsonHelper.readField(
      object,
      'nested_delegates',
      type: 'XdrSorobanDelegateSignature',
    );
    return XdrSorobanDelegateSignature(
      XdrSCAddress.fromXdrJsonValue(jsonAddress),
      XdrSCVal.fromXdrJsonValue(jsonSignature),
      XdrJsonHelper.readArray(
            jsonNestedDelegates,
            type: 'XdrSorobanDelegateSignature',
            key: 'nested_delegates',
          )
          .map<XdrSorobanDelegateSignature>(
            (Object? e) => XdrSorobanDelegateSignature.fromXdrJsonValue(e),
          )
          .toList(),
    );
  }
}
