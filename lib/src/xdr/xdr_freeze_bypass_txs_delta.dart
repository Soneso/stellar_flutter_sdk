// Copyright 2026 The Stellar Flutter SDK Authors. All rights reserved.
// Use of this source code is governed by a license that can be
// found in the LICENSE file.
// This file is automatically generated by xdrgen. Do not edit manually.

import 'dart:convert';
import 'dart:typed_data';

import 'xdr_data_io.dart';
import 'xdr_hash.dart';
import 'xdr_json_helper.dart';

class XdrFreezeBypassTxsDelta {
  List<XdrHash> _addTxs;
  List<XdrHash> get addTxs => this._addTxs;
  set addTxs(List<XdrHash> value) => this._addTxs = value;

  List<XdrHash> _removeTxs;
  List<XdrHash> get removeTxs => this._removeTxs;
  set removeTxs(List<XdrHash> value) => this._removeTxs = value;

  XdrFreezeBypassTxsDelta(this._addTxs, this._removeTxs);

  static void encode(
    XdrDataOutputStream stream,
    XdrFreezeBypassTxsDelta encodedFreezeBypassTxsDelta,
  ) {
    int addTxssize = encodedFreezeBypassTxsDelta.addTxs.length;
    stream.writeInt(addTxssize);
    for (int i = 0; i < addTxssize; i++) {
      XdrHash.encode(stream, encodedFreezeBypassTxsDelta.addTxs[i]);
    }
    int removeTxssize = encodedFreezeBypassTxsDelta.removeTxs.length;
    stream.writeInt(removeTxssize);
    for (int i = 0; i < removeTxssize; i++) {
      XdrHash.encode(stream, encodedFreezeBypassTxsDelta.removeTxs[i]);
    }
  }

  static XdrFreezeBypassTxsDelta decode(XdrDataInputStream stream) {
    int addTxssize = stream.readInt();
    List<XdrHash> addTxs = List<XdrHash>.empty(growable: true);
    for (int i = 0; i < addTxssize; i++) {
      addTxs.add(XdrHash.decode(stream));
    }
    int removeTxssize = stream.readInt();
    List<XdrHash> removeTxs = List<XdrHash>.empty(growable: true);
    for (int i = 0; i < removeTxssize; i++) {
      removeTxs.add(XdrHash.decode(stream));
    }
    return XdrFreezeBypassTxsDelta(addTxs, removeTxs);
  }

  String toBase64EncodedXdrString() {
    XdrDataOutputStream xdrOutputStream = XdrDataOutputStream();
    XdrFreezeBypassTxsDelta.encode(xdrOutputStream, this);
    return base64Encode(xdrOutputStream.bytes);
  }

  static XdrFreezeBypassTxsDelta fromBase64EncodedXdrString(
    String base64Encoded,
  ) {
    Uint8List bytes = base64Decode(base64Encoded);
    return XdrFreezeBypassTxsDelta.decode(XdrDataInputStream(bytes));
  }

  /// Returns the SEP-0051 XDR-JSON rendering of this value.
  String toXdrJson() => XdrJsonHelper.encodeDocument(
    toXdrJsonValue(),
    type: 'XdrFreezeBypassTxsDelta',
  );

  /// Parses the SEP-0051 XDR-JSON rendering of a XdrFreezeBypassTxsDelta.
  static XdrFreezeBypassTxsDelta fromXdrJson(String json) => fromXdrJsonValue(
    XdrJsonHelper.decodeDocument(json, type: 'XdrFreezeBypassTxsDelta'),
  );

  /// Returns the SEP-0051 rendering of this XdrFreezeBypassTxsDelta.
  Object? toXdrJsonValue() => <String, Object?>{
    'add_txs': XdrJsonHelper.array<XdrHash>(
      _addTxs,
      (XdrHash v) => v.toXdrJsonValue(),
      type: 'XdrFreezeBypassTxsDelta',
      key: 'add_txs',
    ),
    'remove_txs': XdrJsonHelper.array<XdrHash>(
      _removeTxs,
      (XdrHash v) => v.toXdrJsonValue(),
      type: 'XdrFreezeBypassTxsDelta',
      key: 'remove_txs',
    ),
  };

  /// Reads a XdrFreezeBypassTxsDelta from its SEP-0051 rendering.
  static XdrFreezeBypassTxsDelta fromXdrJsonValue(Object? value) {
    final Map<String, dynamic> object = XdrJsonHelper.readObject(
      value,
      type: 'XdrFreezeBypassTxsDelta',
      allowedKeys: const <String>{'add_txs', 'remove_txs'},
    );
    final Object? jsonAddTxs = XdrJsonHelper.readField(
      object,
      'add_txs',
      type: 'XdrFreezeBypassTxsDelta',
    );
    final Object? jsonRemoveTxs = XdrJsonHelper.readField(
      object,
      'remove_txs',
      type: 'XdrFreezeBypassTxsDelta',
    );
    return XdrFreezeBypassTxsDelta(
      XdrJsonHelper.readArray(
        jsonAddTxs,
        type: 'XdrFreezeBypassTxsDelta',
        key: 'add_txs',
      ).map<XdrHash>((Object? e) => XdrHash.fromXdrJsonValue(e)).toList(),
      XdrJsonHelper.readArray(
        jsonRemoveTxs,
        type: 'XdrFreezeBypassTxsDelta',
        key: 'remove_txs',
      ).map<XdrHash>((Object? e) => XdrHash.fromXdrJsonValue(e)).toList(),
    );
  }
}
