// Copyright 2026 The Stellar Flutter SDK Authors. All rights reserved.
// Use of this source code is governed by a license that can be
// found in the LICENSE file.
// This file is automatically generated by xdrgen. Do not edit manually.

import 'dart:convert';
import 'dart:typed_data';

import 'xdr_account_id.dart';
import 'xdr_data_io.dart';
import 'xdr_int64.dart';
import 'xdr_json_helper.dart';

class XdrInflationPayout {
  XdrAccountID _destination;
  XdrAccountID get destination => this._destination;
  set destination(XdrAccountID value) => this._destination = value;

  XdrInt64 _amount;
  XdrInt64 get amount => this._amount;
  set amount(XdrInt64 value) => this._amount = value;

  XdrInflationPayout(this._destination, this._amount);

  static void encode(
    XdrDataOutputStream stream,
    XdrInflationPayout encodedInflationPayout,
  ) {
    XdrAccountID.encode(stream, encodedInflationPayout.destination);
    XdrInt64.encode(stream, encodedInflationPayout.amount);
  }

  static XdrInflationPayout decode(XdrDataInputStream stream) {
    XdrAccountID destination = XdrAccountID.decode(stream);
    XdrInt64 amount = XdrInt64.decode(stream);
    return XdrInflationPayout(destination, amount);
  }

  String toBase64EncodedXdrString() {
    XdrDataOutputStream xdrOutputStream = XdrDataOutputStream();
    XdrInflationPayout.encode(xdrOutputStream, this);
    return base64Encode(xdrOutputStream.bytes);
  }

  static XdrInflationPayout fromBase64EncodedXdrString(String base64Encoded) {
    Uint8List bytes = base64Decode(base64Encoded);
    return XdrInflationPayout.decode(XdrDataInputStream(bytes));
  }

  /// Returns the SEP-0051 XDR-JSON rendering of this value.
  String toXdrJson() => XdrJsonHelper.encodeDocument(
    toXdrJsonValue(),
    type: 'XdrInflationPayout',
  );

  /// Parses the SEP-0051 XDR-JSON rendering of a XdrInflationPayout.
  static XdrInflationPayout fromXdrJson(String json) => fromXdrJsonValue(
    XdrJsonHelper.decodeDocument(json, type: 'XdrInflationPayout'),
  );

  /// Returns the SEP-0051 rendering of this XdrInflationPayout.
  Object? toXdrJsonValue() => <String, Object?>{
    'destination': _destination.toXdrJsonValue(),
    'amount': _amount.toXdrJsonValue(),
  };

  /// Reads a XdrInflationPayout from its SEP-0051 rendering.
  static XdrInflationPayout fromXdrJsonValue(Object? value) {
    final Map<String, dynamic> object = XdrJsonHelper.readObject(
      value,
      type: 'XdrInflationPayout',
      allowedKeys: const <String>{'destination', 'amount'},
    );
    final Object? jsonDestination = XdrJsonHelper.readField(
      object,
      'destination',
      type: 'XdrInflationPayout',
    );
    final Object? jsonAmount = XdrJsonHelper.readField(
      object,
      'amount',
      type: 'XdrInflationPayout',
    );
    return XdrInflationPayout(
      XdrAccountID.fromXdrJsonValue(jsonDestination),
      XdrInt64.fromXdrJsonValue(jsonAmount),
    );
  }
}
