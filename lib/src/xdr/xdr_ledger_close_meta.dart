// Copyright 2026 The Stellar Flutter SDK Authors. All rights reserved.
// Use of this source code is governed by a license that can be
// found in the LICENSE file.
// This file is automatically generated by xdrgen. Do not edit manually.

import 'dart:convert';
import 'dart:typed_data';

import 'txrep_helper.dart';
import 'xdr_data_io.dart';
import 'xdr_ledger_close_meta_v0.dart';
import 'xdr_ledger_close_meta_v1.dart';
import 'xdr_ledger_close_meta_v2.dart';

class XdrLedgerCloseMeta {
  int _v;

  int get discriminant => this._v;
  set discriminant(int value) => this._v = value;

  /// Alias for [discriminant], the original XDR field name.
  int get v => this._v;
  set v(int value) => this._v = value;

  XdrLedgerCloseMetaV0? _v0;

  XdrLedgerCloseMetaV0? get v0 => this._v0;

  XdrLedgerCloseMetaV1? _v1;

  XdrLedgerCloseMetaV1? get v1 => this._v1;

  XdrLedgerCloseMetaV2? _v2;

  XdrLedgerCloseMetaV2? get v2 => this._v2;

  XdrLedgerCloseMeta(this._v);

  set v0(XdrLedgerCloseMetaV0? value) => this._v0 = value;

  set v1(XdrLedgerCloseMetaV1? value) => this._v1 = value;

  set v2(XdrLedgerCloseMetaV2? value) => this._v2 = value;

  static void encode(
    XdrDataOutputStream stream,
    XdrLedgerCloseMeta encodedLedgerCloseMeta,
  ) {
    stream.writeInt(encodedLedgerCloseMeta.discriminant);
    switch (encodedLedgerCloseMeta.discriminant) {
      case 0:
        XdrLedgerCloseMetaV0.encode(stream, encodedLedgerCloseMeta._v0!);
        break;
      case 1:
        XdrLedgerCloseMetaV1.encode(stream, encodedLedgerCloseMeta._v1!);
        break;
      case 2:
        XdrLedgerCloseMetaV2.encode(stream, encodedLedgerCloseMeta._v2!);
        break;
      default:
        break;
    }
  }

  static XdrLedgerCloseMeta decode(XdrDataInputStream stream) {
    int discriminant = stream.readInt();
    XdrLedgerCloseMeta decodedLedgerCloseMeta = XdrLedgerCloseMeta(
      discriminant,
    );
    switch (decodedLedgerCloseMeta.discriminant) {
      case 0:
        decodedLedgerCloseMeta._v0 = XdrLedgerCloseMetaV0.decode(stream);
        break;
      case 1:
        decodedLedgerCloseMeta._v1 = XdrLedgerCloseMetaV1.decode(stream);
        break;
      case 2:
        decodedLedgerCloseMeta._v2 = XdrLedgerCloseMetaV2.decode(stream);
        break;
      default:
        break;
    }
    return decodedLedgerCloseMeta;
  }

  String toBase64EncodedXdrString() {
    XdrDataOutputStream xdrOutputStream = XdrDataOutputStream();
    XdrLedgerCloseMeta.encode(xdrOutputStream, this);
    return base64Encode(xdrOutputStream.bytes);
  }

  static XdrLedgerCloseMeta fromBase64EncodedXdrString(String base64Encoded) {
    Uint8List bytes = base64Decode(base64Encoded);
    return XdrLedgerCloseMeta.decode(XdrDataInputStream(bytes));
  }

  void toTxRep(String prefix, List<String> lines) {
    lines.add('$prefix.v: $discriminant');
    switch (discriminant) {
      case 0:
        _v0!.toTxRep('$prefix.v0', lines);
        break;
      case 1:
        _v1!.toTxRep('$prefix.v1', lines);
        break;
      case 2:
        _v2!.toTxRep('$prefix.v2', lines);
        break;
      default:
        break;
    }
  }

  static XdrLedgerCloseMeta fromTxRep(Map<String, String> map, String prefix) {
    int disc = TxRepHelper.parseInt(
      TxRepHelper.getValue(map, '$prefix.v') ?? '0',
    );
    XdrLedgerCloseMeta result = XdrLedgerCloseMeta(disc);
    switch (result.discriminant) {
      case 0:
        result._v0 = XdrLedgerCloseMetaV0.fromTxRep(map, '$prefix.v0');
        break;
      case 1:
        result._v1 = XdrLedgerCloseMetaV1.fromTxRep(map, '$prefix.v1');
        break;
      case 2:
        result._v2 = XdrLedgerCloseMetaV2.fromTxRep(map, '$prefix.v2');
        break;
      default:
        break;
    }
    return result;
  }
}
