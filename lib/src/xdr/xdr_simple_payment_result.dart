// Copyright 2026 The Stellar Flutter SDK Authors. All rights reserved.
// Use of this source code is governed by a license that can be
// found in the LICENSE file.
// This file is automatically generated by xdrgen. Do not edit manually.

import 'dart:convert';
import 'dart:typed_data';

import 'xdr_asset.dart';
import 'xdr_data_io.dart';
import 'xdr_int64.dart';
import 'xdr_json_helper.dart';
import 'xdr_muxed_account.dart';

class XdrSimplePaymentResult {
  XdrMuxedAccount _destination;
  XdrMuxedAccount get destination => this._destination;
  set destination(XdrMuxedAccount value) => this._destination = value;

  XdrAsset _asset;
  XdrAsset get asset => this._asset;
  set asset(XdrAsset value) => this._asset = value;

  XdrInt64 _amount;
  XdrInt64 get amount => this._amount;
  set amount(XdrInt64 value) => this._amount = value;

  XdrSimplePaymentResult(this._destination, this._asset, this._amount);

  static void encode(
    XdrDataOutputStream stream,
    XdrSimplePaymentResult encodedSimplePaymentResult,
  ) {
    XdrMuxedAccount.encode(stream, encodedSimplePaymentResult.destination);
    XdrAsset.encode(stream, encodedSimplePaymentResult.asset);
    XdrInt64.encode(stream, encodedSimplePaymentResult.amount);
  }

  static XdrSimplePaymentResult decode(XdrDataInputStream stream) {
    XdrMuxedAccount destination = XdrMuxedAccount.decode(stream);
    XdrAsset asset = XdrAsset.decode(stream);
    XdrInt64 amount = XdrInt64.decode(stream);
    return XdrSimplePaymentResult(destination, asset, amount);
  }

  String toBase64EncodedXdrString() {
    XdrDataOutputStream xdrOutputStream = XdrDataOutputStream();
    XdrSimplePaymentResult.encode(xdrOutputStream, this);
    return base64Encode(xdrOutputStream.bytes);
  }

  static XdrSimplePaymentResult fromBase64EncodedXdrString(
    String base64Encoded,
  ) {
    Uint8List bytes = base64Decode(base64Encoded);
    return XdrSimplePaymentResult.decode(XdrDataInputStream(bytes));
  }

  /// Returns the SEP-0051 XDR-JSON rendering of this value.
  String toXdrJson() => XdrJsonHelper.encodeDocument(
    toXdrJsonValue(),
    type: 'XdrSimplePaymentResult',
  );

  /// Parses the SEP-0051 XDR-JSON rendering of a XdrSimplePaymentResult.
  static XdrSimplePaymentResult fromXdrJson(String json) => fromXdrJsonValue(
    XdrJsonHelper.decodeDocument(json, type: 'XdrSimplePaymentResult'),
  );

  /// Returns the SEP-0051 rendering of this XdrSimplePaymentResult.
  Object? toXdrJsonValue() => <String, Object?>{
    'destination': _destination.toXdrJsonValue(),
    'asset': _asset.toXdrJsonValue(),
    'amount': _amount.toXdrJsonValue(),
  };

  /// Reads a XdrSimplePaymentResult from its SEP-0051 rendering.
  static XdrSimplePaymentResult fromXdrJsonValue(Object? value) {
    final Map<String, dynamic> object = XdrJsonHelper.readObject(
      value,
      type: 'XdrSimplePaymentResult',
      allowedKeys: const <String>{'destination', 'asset', 'amount'},
    );
    final Object? jsonDestination = XdrJsonHelper.readField(
      object,
      'destination',
      type: 'XdrSimplePaymentResult',
    );
    final Object? jsonAsset = XdrJsonHelper.readField(
      object,
      'asset',
      type: 'XdrSimplePaymentResult',
    );
    final Object? jsonAmount = XdrJsonHelper.readField(
      object,
      'amount',
      type: 'XdrSimplePaymentResult',
    );
    return XdrSimplePaymentResult(
      XdrMuxedAccount.fromXdrJsonValue(jsonDestination),
      XdrAsset.fromXdrJsonValue(jsonAsset),
      XdrInt64.fromXdrJsonValue(jsonAmount),
    );
  }
}
