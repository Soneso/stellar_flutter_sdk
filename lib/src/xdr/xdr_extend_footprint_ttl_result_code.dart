// Copyright 2026 The Stellar Flutter SDK Authors. All rights reserved.
// Use of this source code is governed by a license that can be
// found in the LICENSE file.
// This file is automatically generated by xdrgen. Do not edit manually.

import 'dart:convert';
import 'dart:typed_data';

import 'xdr_data_io.dart';
import 'xdr_json_helper.dart';

class XdrExtendFootprintTTLResultCode {
  final _value;
  const XdrExtendFootprintTTLResultCode._internal(this._value);
  toString() => 'ExtendFootprintTTLResultCode.$_value';
  XdrExtendFootprintTTLResultCode(this._value);
  get value => this._value;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is XdrExtendFootprintTTLResultCode && _value == other._value;

  @override
  int get hashCode => _value.hashCode;

  static const EXTEND_FOOTPRINT_TTL_SUCCESS =
      const XdrExtendFootprintTTLResultCode._internal(0);
  static const EXTEND_FOOTPRINT_TTL_MALFORMED =
      const XdrExtendFootprintTTLResultCode._internal(-1);
  static const EXTEND_FOOTPRINT_TTL_RESOURCE_LIMIT_EXCEEDED =
      const XdrExtendFootprintTTLResultCode._internal(-2);
  static const EXTEND_FOOTPRINT_TTL_INSUFFICIENT_REFUNDABLE_FEE =
      const XdrExtendFootprintTTLResultCode._internal(-3);

  static XdrExtendFootprintTTLResultCode decode(XdrDataInputStream stream) {
    int value = stream.readInt();
    switch (value) {
      case 0:
        return EXTEND_FOOTPRINT_TTL_SUCCESS;
      case -1:
        return EXTEND_FOOTPRINT_TTL_MALFORMED;
      case -2:
        return EXTEND_FOOTPRINT_TTL_RESOURCE_LIMIT_EXCEEDED;
      case -3:
        return EXTEND_FOOTPRINT_TTL_INSUFFICIENT_REFUNDABLE_FEE;
      default:
        throw Exception("Unknown enum value: $value");
    }
  }

  static void encode(
    XdrDataOutputStream stream,
    XdrExtendFootprintTTLResultCode value,
  ) {
    stream.writeInt(value.value);
  }

  String toBase64EncodedXdrString() {
    XdrDataOutputStream xdrOutputStream = XdrDataOutputStream();
    XdrExtendFootprintTTLResultCode.encode(xdrOutputStream, this);
    return base64Encode(xdrOutputStream.bytes);
  }

  static XdrExtendFootprintTTLResultCode fromBase64EncodedXdrString(
    String base64Encoded,
  ) {
    Uint8List bytes = base64Decode(base64Encoded);
    return XdrExtendFootprintTTLResultCode.decode(XdrDataInputStream(bytes));
  }

  /// Returns the SEP-0051 XDR-JSON rendering of this value.
  String toXdrJson() => XdrJsonHelper.encodeDocument(
    toXdrJsonValue(),
    type: 'XdrExtendFootprintTTLResultCode',
  );

  /// Parses the SEP-0051 XDR-JSON rendering of a XdrExtendFootprintTTLResultCode.
  static XdrExtendFootprintTTLResultCode fromXdrJson(String json) =>
      fromXdrJsonValue(
        XdrJsonHelper.decodeDocument(
          json,
          type: 'XdrExtendFootprintTTLResultCode',
        ),
      );

  /// Returns this member's SEP-0051 name.
  Object? toXdrJsonValue() {
    switch (_value) {
      case 0:
        return 'success';
      case -1:
        return 'malformed';
      case -2:
        return 'resource_limit_exceeded';
      case -3:
        return 'insufficient_refundable_fee';
      default:
        XdrJsonHelper.fail(
          'XdrExtendFootprintTTLResultCode',
          'holds the unknown value $_value',
        );
    }
  }

  /// Reads a XdrExtendFootprintTTLResultCode from its SEP-0051 name.
  static XdrExtendFootprintTTLResultCode fromXdrJsonValue(Object? value) {
    if (value is String) {
      switch (value) {
        case 'success':
          return XdrExtendFootprintTTLResultCode.EXTEND_FOOTPRINT_TTL_SUCCESS;
        case 'malformed':
          return XdrExtendFootprintTTLResultCode.EXTEND_FOOTPRINT_TTL_MALFORMED;
        case 'resource_limit_exceeded':
          return XdrExtendFootprintTTLResultCode
              .EXTEND_FOOTPRINT_TTL_RESOURCE_LIMIT_EXCEEDED;
        case 'insufficient_refundable_fee':
          return XdrExtendFootprintTTLResultCode
              .EXTEND_FOOTPRINT_TTL_INSUFFICIENT_REFUNDABLE_FEE;
      }
    }
    XdrJsonHelper.fail(
      'XdrExtendFootprintTTLResultCode',
      'expects one of its member names but found ${XdrJsonHelper.preview(value)}',
    );
  }
}
