// AUTO-GENERATED - DO NOT EDIT
// Generated by tools/xdr-generator/test/generate_json_tests.rb

import 'dart:typed_data';
import 'package:flutter_test/flutter_test.dart';
import 'package:stellar_flutter_sdk/stellar_flutter_sdk.dart';

void main() {
  group('SEP-0051 contract renderings', () {
    test('XdrSCBytes round trip', () {
      var original = XdrSCBytes(Uint8List.fromList([1, 2, 3]));
      var json = original.toXdrJson();
      var fromDocument = XdrSCBytes.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCBytes.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCValType SCV_BOOL renders as bool', () {
      var member = XdrSCValType.SCV_BOOL;
      expect(member.value, 0);
      expect(member.toXdrJsonValue(), 'bool');
      expect(member.toXdrJson(), '"bool"');
      var parsed = XdrSCValType.fromXdrJson('"bool"');
      expect(parsed.value, 0);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('bool').value, 0);
    });
    test('XdrSCValType SCV_VOID renders as void', () {
      var member = XdrSCValType.SCV_VOID;
      expect(member.value, 1);
      expect(member.toXdrJsonValue(), 'void');
      expect(member.toXdrJson(), '"void"');
      var parsed = XdrSCValType.fromXdrJson('"void"');
      expect(parsed.value, 1);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('void').value, 1);
    });
    test('XdrSCValType SCV_ERROR renders as error', () {
      var member = XdrSCValType.SCV_ERROR;
      expect(member.value, 2);
      expect(member.toXdrJsonValue(), 'error');
      expect(member.toXdrJson(), '"error"');
      var parsed = XdrSCValType.fromXdrJson('"error"');
      expect(parsed.value, 2);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('error').value, 2);
    });
    test('XdrSCValType SCV_U32 renders as u32', () {
      var member = XdrSCValType.SCV_U32;
      expect(member.value, 3);
      expect(member.toXdrJsonValue(), 'u32');
      expect(member.toXdrJson(), '"u32"');
      var parsed = XdrSCValType.fromXdrJson('"u32"');
      expect(parsed.value, 3);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('u32').value, 3);
    });
    test('XdrSCValType SCV_I32 renders as i32', () {
      var member = XdrSCValType.SCV_I32;
      expect(member.value, 4);
      expect(member.toXdrJsonValue(), 'i32');
      expect(member.toXdrJson(), '"i32"');
      var parsed = XdrSCValType.fromXdrJson('"i32"');
      expect(parsed.value, 4);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('i32').value, 4);
    });
    test('XdrSCValType SCV_U64 renders as u64', () {
      var member = XdrSCValType.SCV_U64;
      expect(member.value, 5);
      expect(member.toXdrJsonValue(), 'u64');
      expect(member.toXdrJson(), '"u64"');
      var parsed = XdrSCValType.fromXdrJson('"u64"');
      expect(parsed.value, 5);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('u64').value, 5);
    });
    test('XdrSCValType SCV_I64 renders as i64', () {
      var member = XdrSCValType.SCV_I64;
      expect(member.value, 6);
      expect(member.toXdrJsonValue(), 'i64');
      expect(member.toXdrJson(), '"i64"');
      var parsed = XdrSCValType.fromXdrJson('"i64"');
      expect(parsed.value, 6);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('i64').value, 6);
    });
    test('XdrSCValType SCV_TIMEPOINT renders as timepoint', () {
      var member = XdrSCValType.SCV_TIMEPOINT;
      expect(member.value, 7);
      expect(member.toXdrJsonValue(), 'timepoint');
      expect(member.toXdrJson(), '"timepoint"');
      var parsed = XdrSCValType.fromXdrJson('"timepoint"');
      expect(parsed.value, 7);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('timepoint').value, 7);
    });
    test('XdrSCValType SCV_DURATION renders as duration', () {
      var member = XdrSCValType.SCV_DURATION;
      expect(member.value, 8);
      expect(member.toXdrJsonValue(), 'duration');
      expect(member.toXdrJson(), '"duration"');
      var parsed = XdrSCValType.fromXdrJson('"duration"');
      expect(parsed.value, 8);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('duration').value, 8);
    });
    test('XdrSCValType SCV_U128 renders as u128', () {
      var member = XdrSCValType.SCV_U128;
      expect(member.value, 9);
      expect(member.toXdrJsonValue(), 'u128');
      expect(member.toXdrJson(), '"u128"');
      var parsed = XdrSCValType.fromXdrJson('"u128"');
      expect(parsed.value, 9);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('u128').value, 9);
    });
    test('XdrSCValType SCV_I128 renders as i128', () {
      var member = XdrSCValType.SCV_I128;
      expect(member.value, 10);
      expect(member.toXdrJsonValue(), 'i128');
      expect(member.toXdrJson(), '"i128"');
      var parsed = XdrSCValType.fromXdrJson('"i128"');
      expect(parsed.value, 10);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('i128').value, 10);
    });
    test('XdrSCValType SCV_U256 renders as u256', () {
      var member = XdrSCValType.SCV_U256;
      expect(member.value, 11);
      expect(member.toXdrJsonValue(), 'u256');
      expect(member.toXdrJson(), '"u256"');
      var parsed = XdrSCValType.fromXdrJson('"u256"');
      expect(parsed.value, 11);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('u256').value, 11);
    });
    test('XdrSCValType SCV_I256 renders as i256', () {
      var member = XdrSCValType.SCV_I256;
      expect(member.value, 12);
      expect(member.toXdrJsonValue(), 'i256');
      expect(member.toXdrJson(), '"i256"');
      var parsed = XdrSCValType.fromXdrJson('"i256"');
      expect(parsed.value, 12);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('i256').value, 12);
    });
    test('XdrSCValType SCV_BYTES renders as bytes', () {
      var member = XdrSCValType.SCV_BYTES;
      expect(member.value, 13);
      expect(member.toXdrJsonValue(), 'bytes');
      expect(member.toXdrJson(), '"bytes"');
      var parsed = XdrSCValType.fromXdrJson('"bytes"');
      expect(parsed.value, 13);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('bytes').value, 13);
    });
    test('XdrSCValType SCV_STRING renders as string', () {
      var member = XdrSCValType.SCV_STRING;
      expect(member.value, 14);
      expect(member.toXdrJsonValue(), 'string');
      expect(member.toXdrJson(), '"string"');
      var parsed = XdrSCValType.fromXdrJson('"string"');
      expect(parsed.value, 14);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('string').value, 14);
    });
    test('XdrSCValType SCV_SYMBOL renders as symbol', () {
      var member = XdrSCValType.SCV_SYMBOL;
      expect(member.value, 15);
      expect(member.toXdrJsonValue(), 'symbol');
      expect(member.toXdrJson(), '"symbol"');
      var parsed = XdrSCValType.fromXdrJson('"symbol"');
      expect(parsed.value, 15);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('symbol').value, 15);
    });
    test('XdrSCValType SCV_VEC renders as vec', () {
      var member = XdrSCValType.SCV_VEC;
      expect(member.value, 16);
      expect(member.toXdrJsonValue(), 'vec');
      expect(member.toXdrJson(), '"vec"');
      var parsed = XdrSCValType.fromXdrJson('"vec"');
      expect(parsed.value, 16);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('vec').value, 16);
    });
    test('XdrSCValType SCV_MAP renders as map', () {
      var member = XdrSCValType.SCV_MAP;
      expect(member.value, 17);
      expect(member.toXdrJsonValue(), 'map');
      expect(member.toXdrJson(), '"map"');
      var parsed = XdrSCValType.fromXdrJson('"map"');
      expect(parsed.value, 17);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('map').value, 17);
    });
    test('XdrSCValType SCV_ADDRESS renders as address', () {
      var member = XdrSCValType.SCV_ADDRESS;
      expect(member.value, 18);
      expect(member.toXdrJsonValue(), 'address');
      expect(member.toXdrJson(), '"address"');
      var parsed = XdrSCValType.fromXdrJson('"address"');
      expect(parsed.value, 18);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('address').value, 18);
    });
    test('XdrSCValType SCV_CONTRACT_INSTANCE renders as contract_instance', () {
      var member = XdrSCValType.SCV_CONTRACT_INSTANCE;
      expect(member.value, 19);
      expect(member.toXdrJsonValue(), 'contract_instance');
      expect(member.toXdrJson(), '"contract_instance"');
      var parsed = XdrSCValType.fromXdrJson('"contract_instance"');
      expect(parsed.value, 19);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('contract_instance').value, 19);
    });
    test(
      'XdrSCValType SCV_LEDGER_KEY_CONTRACT_INSTANCE renders as ledger_key_contract_instance',
      () {
        var member = XdrSCValType.SCV_LEDGER_KEY_CONTRACT_INSTANCE;
        expect(member.value, 20);
        expect(member.toXdrJsonValue(), 'ledger_key_contract_instance');
        expect(member.toXdrJson(), '"ledger_key_contract_instance"');
        var parsed = XdrSCValType.fromXdrJson('"ledger_key_contract_instance"');
        expect(parsed.value, 20);
        expect(
          parsed.toBase64EncodedXdrString(),
          member.toBase64EncodedXdrString(),
        );
        expect(
          XdrSCValType.fromXdrJsonValue('ledger_key_contract_instance').value,
          20,
        );
      },
    );
    test('XdrSCValType SCV_LEDGER_KEY_NONCE renders as ledger_key_nonce', () {
      var member = XdrSCValType.SCV_LEDGER_KEY_NONCE;
      expect(member.value, 21);
      expect(member.toXdrJsonValue(), 'ledger_key_nonce');
      expect(member.toXdrJson(), '"ledger_key_nonce"');
      var parsed = XdrSCValType.fromXdrJson('"ledger_key_nonce"');
      expect(parsed.value, 21);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('ledger_key_nonce').value, 21);
    });
    test('XdrSCValType SCV_EXECUTABLE_TAG renders as executable_tag', () {
      var member = XdrSCValType.SCV_EXECUTABLE_TAG;
      expect(member.value, 22);
      expect(member.toXdrJsonValue(), 'executable_tag');
      expect(member.toXdrJson(), '"executable_tag"');
      var parsed = XdrSCValType.fromXdrJson('"executable_tag"');
      expect(parsed.value, 22);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCValType.fromXdrJsonValue('executable_tag').value, 22);
    });
    test('XdrSCValType rejects an undeclared member name', () {
      expect(
        () => XdrSCValType.fromXdrJsonValue('xdr_json_undeclared_member'),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSCValType rejects an undeclared value', () {
      expect(
        () => XdrSCValType(23).toXdrJsonValue(),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSCErrorType SCE_CONTRACT renders as contract', () {
      var member = XdrSCErrorType.SCE_CONTRACT;
      expect(member.value, 0);
      expect(member.toXdrJsonValue(), 'contract');
      expect(member.toXdrJson(), '"contract"');
      var parsed = XdrSCErrorType.fromXdrJson('"contract"');
      expect(parsed.value, 0);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorType.fromXdrJsonValue('contract').value, 0);
    });
    test('XdrSCErrorType SCE_WASM_VM renders as wasm_vm', () {
      var member = XdrSCErrorType.SCE_WASM_VM;
      expect(member.value, 1);
      expect(member.toXdrJsonValue(), 'wasm_vm');
      expect(member.toXdrJson(), '"wasm_vm"');
      var parsed = XdrSCErrorType.fromXdrJson('"wasm_vm"');
      expect(parsed.value, 1);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorType.fromXdrJsonValue('wasm_vm').value, 1);
    });
    test('XdrSCErrorType SCE_CONTEXT renders as context', () {
      var member = XdrSCErrorType.SCE_CONTEXT;
      expect(member.value, 2);
      expect(member.toXdrJsonValue(), 'context');
      expect(member.toXdrJson(), '"context"');
      var parsed = XdrSCErrorType.fromXdrJson('"context"');
      expect(parsed.value, 2);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorType.fromXdrJsonValue('context').value, 2);
    });
    test('XdrSCErrorType SCE_STORAGE renders as storage', () {
      var member = XdrSCErrorType.SCE_STORAGE;
      expect(member.value, 3);
      expect(member.toXdrJsonValue(), 'storage');
      expect(member.toXdrJson(), '"storage"');
      var parsed = XdrSCErrorType.fromXdrJson('"storage"');
      expect(parsed.value, 3);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorType.fromXdrJsonValue('storage').value, 3);
    });
    test('XdrSCErrorType SCE_OBJECT renders as object', () {
      var member = XdrSCErrorType.SCE_OBJECT;
      expect(member.value, 4);
      expect(member.toXdrJsonValue(), 'object');
      expect(member.toXdrJson(), '"object"');
      var parsed = XdrSCErrorType.fromXdrJson('"object"');
      expect(parsed.value, 4);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorType.fromXdrJsonValue('object').value, 4);
    });
    test('XdrSCErrorType SCE_CRYPTO renders as crypto', () {
      var member = XdrSCErrorType.SCE_CRYPTO;
      expect(member.value, 5);
      expect(member.toXdrJsonValue(), 'crypto');
      expect(member.toXdrJson(), '"crypto"');
      var parsed = XdrSCErrorType.fromXdrJson('"crypto"');
      expect(parsed.value, 5);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorType.fromXdrJsonValue('crypto').value, 5);
    });
    test('XdrSCErrorType SCE_EVENTS renders as events', () {
      var member = XdrSCErrorType.SCE_EVENTS;
      expect(member.value, 6);
      expect(member.toXdrJsonValue(), 'events');
      expect(member.toXdrJson(), '"events"');
      var parsed = XdrSCErrorType.fromXdrJson('"events"');
      expect(parsed.value, 6);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorType.fromXdrJsonValue('events').value, 6);
    });
    test('XdrSCErrorType SCE_BUDGET renders as budget', () {
      var member = XdrSCErrorType.SCE_BUDGET;
      expect(member.value, 7);
      expect(member.toXdrJsonValue(), 'budget');
      expect(member.toXdrJson(), '"budget"');
      var parsed = XdrSCErrorType.fromXdrJson('"budget"');
      expect(parsed.value, 7);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorType.fromXdrJsonValue('budget').value, 7);
    });
    test('XdrSCErrorType SCE_VALUE renders as value', () {
      var member = XdrSCErrorType.SCE_VALUE;
      expect(member.value, 8);
      expect(member.toXdrJsonValue(), 'value');
      expect(member.toXdrJson(), '"value"');
      var parsed = XdrSCErrorType.fromXdrJson('"value"');
      expect(parsed.value, 8);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorType.fromXdrJsonValue('value').value, 8);
    });
    test('XdrSCErrorType SCE_AUTH renders as auth', () {
      var member = XdrSCErrorType.SCE_AUTH;
      expect(member.value, 9);
      expect(member.toXdrJsonValue(), 'auth');
      expect(member.toXdrJson(), '"auth"');
      var parsed = XdrSCErrorType.fromXdrJson('"auth"');
      expect(parsed.value, 9);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorType.fromXdrJsonValue('auth').value, 9);
    });
    test('XdrSCErrorType rejects an undeclared member name', () {
      expect(
        () => XdrSCErrorType.fromXdrJsonValue('xdr_json_undeclared_member'),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSCErrorType rejects an undeclared value', () {
      expect(
        () => XdrSCErrorType(10).toXdrJsonValue(),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSCErrorCode SCEC_ARITH_DOMAIN renders as arith_domain', () {
      var member = XdrSCErrorCode.SCEC_ARITH_DOMAIN;
      expect(member.value, 0);
      expect(member.toXdrJsonValue(), 'arith_domain');
      expect(member.toXdrJson(), '"arith_domain"');
      var parsed = XdrSCErrorCode.fromXdrJson('"arith_domain"');
      expect(parsed.value, 0);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorCode.fromXdrJsonValue('arith_domain').value, 0);
    });
    test('XdrSCErrorCode SCEC_INDEX_BOUNDS renders as index_bounds', () {
      var member = XdrSCErrorCode.SCEC_INDEX_BOUNDS;
      expect(member.value, 1);
      expect(member.toXdrJsonValue(), 'index_bounds');
      expect(member.toXdrJson(), '"index_bounds"');
      var parsed = XdrSCErrorCode.fromXdrJson('"index_bounds"');
      expect(parsed.value, 1);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorCode.fromXdrJsonValue('index_bounds').value, 1);
    });
    test('XdrSCErrorCode SCEC_INVALID_INPUT renders as invalid_input', () {
      var member = XdrSCErrorCode.SCEC_INVALID_INPUT;
      expect(member.value, 2);
      expect(member.toXdrJsonValue(), 'invalid_input');
      expect(member.toXdrJson(), '"invalid_input"');
      var parsed = XdrSCErrorCode.fromXdrJson('"invalid_input"');
      expect(parsed.value, 2);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorCode.fromXdrJsonValue('invalid_input').value, 2);
    });
    test('XdrSCErrorCode SCEC_MISSING_VALUE renders as missing_value', () {
      var member = XdrSCErrorCode.SCEC_MISSING_VALUE;
      expect(member.value, 3);
      expect(member.toXdrJsonValue(), 'missing_value');
      expect(member.toXdrJson(), '"missing_value"');
      var parsed = XdrSCErrorCode.fromXdrJson('"missing_value"');
      expect(parsed.value, 3);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorCode.fromXdrJsonValue('missing_value').value, 3);
    });
    test('XdrSCErrorCode SCEC_EXISTING_VALUE renders as existing_value', () {
      var member = XdrSCErrorCode.SCEC_EXISTING_VALUE;
      expect(member.value, 4);
      expect(member.toXdrJsonValue(), 'existing_value');
      expect(member.toXdrJson(), '"existing_value"');
      var parsed = XdrSCErrorCode.fromXdrJson('"existing_value"');
      expect(parsed.value, 4);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorCode.fromXdrJsonValue('existing_value').value, 4);
    });
    test('XdrSCErrorCode SCEC_EXCEEDED_LIMIT renders as exceeded_limit', () {
      var member = XdrSCErrorCode.SCEC_EXCEEDED_LIMIT;
      expect(member.value, 5);
      expect(member.toXdrJsonValue(), 'exceeded_limit');
      expect(member.toXdrJson(), '"exceeded_limit"');
      var parsed = XdrSCErrorCode.fromXdrJson('"exceeded_limit"');
      expect(parsed.value, 5);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorCode.fromXdrJsonValue('exceeded_limit').value, 5);
    });
    test('XdrSCErrorCode SCEC_INVALID_ACTION renders as invalid_action', () {
      var member = XdrSCErrorCode.SCEC_INVALID_ACTION;
      expect(member.value, 6);
      expect(member.toXdrJsonValue(), 'invalid_action');
      expect(member.toXdrJson(), '"invalid_action"');
      var parsed = XdrSCErrorCode.fromXdrJson('"invalid_action"');
      expect(parsed.value, 6);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorCode.fromXdrJsonValue('invalid_action').value, 6);
    });
    test('XdrSCErrorCode SCEC_INTERNAL_ERROR renders as internal_error', () {
      var member = XdrSCErrorCode.SCEC_INTERNAL_ERROR;
      expect(member.value, 7);
      expect(member.toXdrJsonValue(), 'internal_error');
      expect(member.toXdrJson(), '"internal_error"');
      var parsed = XdrSCErrorCode.fromXdrJson('"internal_error"');
      expect(parsed.value, 7);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorCode.fromXdrJsonValue('internal_error').value, 7);
    });
    test('XdrSCErrorCode SCEC_UNEXPECTED_TYPE renders as unexpected_type', () {
      var member = XdrSCErrorCode.SCEC_UNEXPECTED_TYPE;
      expect(member.value, 8);
      expect(member.toXdrJsonValue(), 'unexpected_type');
      expect(member.toXdrJson(), '"unexpected_type"');
      var parsed = XdrSCErrorCode.fromXdrJson('"unexpected_type"');
      expect(parsed.value, 8);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorCode.fromXdrJsonValue('unexpected_type').value, 8);
    });
    test('XdrSCErrorCode SCEC_UNEXPECTED_SIZE renders as unexpected_size', () {
      var member = XdrSCErrorCode.SCEC_UNEXPECTED_SIZE;
      expect(member.value, 9);
      expect(member.toXdrJsonValue(), 'unexpected_size');
      expect(member.toXdrJson(), '"unexpected_size"');
      var parsed = XdrSCErrorCode.fromXdrJson('"unexpected_size"');
      expect(parsed.value, 9);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCErrorCode.fromXdrJsonValue('unexpected_size').value, 9);
    });
    test('XdrSCErrorCode rejects an undeclared member name', () {
      expect(
        () => XdrSCErrorCode.fromXdrJsonValue('xdr_json_undeclared_member'),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSCErrorCode rejects an undeclared value', () {
      expect(
        () => XdrSCErrorCode(10).toXdrJsonValue(),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSCError contract arm round trip', () {
      var original = XdrSCError(XdrSCErrorType.SCE_CONTRACT);
      original.contractCode = XdrUint32(42);
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'contract',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCError.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCError.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCError wasm_vm arm round trip', () {
      var original = XdrSCError(XdrSCErrorType.SCE_WASM_VM);
      original.code = XdrSCErrorCode.SCEC_ARITH_DOMAIN;
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'wasm_vm',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCError.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCError.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCError context arm round trip', () {
      var original = XdrSCError(XdrSCErrorType.SCE_CONTEXT);
      original.code = XdrSCErrorCode.SCEC_ARITH_DOMAIN;
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'context',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCError.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCError.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCError storage arm round trip', () {
      var original = XdrSCError(XdrSCErrorType.SCE_STORAGE);
      original.code = XdrSCErrorCode.SCEC_ARITH_DOMAIN;
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'storage',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCError.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCError.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCError object arm round trip', () {
      var original = XdrSCError(XdrSCErrorType.SCE_OBJECT);
      original.code = XdrSCErrorCode.SCEC_ARITH_DOMAIN;
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'object',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCError.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCError.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCError crypto arm round trip', () {
      var original = XdrSCError(XdrSCErrorType.SCE_CRYPTO);
      original.code = XdrSCErrorCode.SCEC_ARITH_DOMAIN;
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'crypto',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCError.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCError.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCError events arm round trip', () {
      var original = XdrSCError(XdrSCErrorType.SCE_EVENTS);
      original.code = XdrSCErrorCode.SCEC_ARITH_DOMAIN;
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'events',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCError.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCError.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCError budget arm round trip', () {
      var original = XdrSCError(XdrSCErrorType.SCE_BUDGET);
      original.code = XdrSCErrorCode.SCEC_ARITH_DOMAIN;
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'budget',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCError.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCError.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCError value arm round trip', () {
      var original = XdrSCError(XdrSCErrorType.SCE_VALUE);
      original.code = XdrSCErrorCode.SCEC_ARITH_DOMAIN;
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'value',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCError.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCError.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCError auth arm round trip', () {
      var original = XdrSCError(XdrSCErrorType.SCE_AUTH);
      original.code = XdrSCErrorCode.SCEC_ARITH_DOMAIN;
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'auth',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCError.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCError.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCError rejects an undeclared discriminant', () {
      expect(
        () => XdrSCError(XdrSCErrorType(10)).toXdrJsonValue(),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSCError rejects an undeclared arm key', () {
      expect(
        () => XdrSCError.fromXdrJsonValue(<String, Object?>{
          'xdr_json_undeclared_arm': null,
        }),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrUInt128Parts round trip', () {
      var original = XdrUInt128Parts(
        XdrUint64(BigInt.from(123456)),
        XdrUint64(BigInt.from(123456)),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrUInt128Parts.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrUInt128Parts.fromXdrJsonValue(
        original.toXdrJsonValue(),
      );
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrUInt128Parts rejects a rendering that is not a string', () {
      expect(
        () => XdrUInt128Parts.fromXdrJsonValue(<String, Object?>{}),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrInt128Parts round trip', () {
      var original = XdrInt128Parts(
        XdrInt64(BigInt.from(654321)),
        XdrUint64(BigInt.from(123456)),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrInt128Parts.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrInt128Parts.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrInt128Parts rejects a rendering that is not a string', () {
      expect(
        () => XdrInt128Parts.fromXdrJsonValue(<String, Object?>{}),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrUInt256Parts round trip', () {
      var original = XdrUInt256Parts(
        XdrUint64(BigInt.from(123456)),
        XdrUint64(BigInt.from(123456)),
        XdrUint64(BigInt.from(123456)),
        XdrUint64(BigInt.from(123456)),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrUInt256Parts.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrUInt256Parts.fromXdrJsonValue(
        original.toXdrJsonValue(),
      );
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrUInt256Parts rejects a rendering that is not a string', () {
      expect(
        () => XdrUInt256Parts.fromXdrJsonValue(<String, Object?>{}),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrInt256Parts round trip', () {
      var original = XdrInt256Parts(
        XdrInt64(BigInt.from(654321)),
        XdrUint64(BigInt.from(123456)),
        XdrUint64(BigInt.from(123456)),
        XdrUint64(BigInt.from(123456)),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrInt256Parts.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrInt256Parts.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrInt256Parts rejects a rendering that is not a string', () {
      expect(
        () => XdrInt256Parts.fromXdrJsonValue(<String, Object?>{}),
        throwsA(isA<FormatException>()),
      );
    });
    test(
      'XdrContractExecutableType CONTRACT_EXECUTABLE_WASM renders as wasm',
      () {
        var member = XdrContractExecutableType.CONTRACT_EXECUTABLE_WASM;
        expect(member.value, 0);
        expect(member.toXdrJsonValue(), 'wasm');
        expect(member.toXdrJson(), '"wasm"');
        var parsed = XdrContractExecutableType.fromXdrJson('"wasm"');
        expect(parsed.value, 0);
        expect(
          parsed.toBase64EncodedXdrString(),
          member.toBase64EncodedXdrString(),
        );
        expect(XdrContractExecutableType.fromXdrJsonValue('wasm').value, 0);
      },
    );
    test(
      'XdrContractExecutableType CONTRACT_EXECUTABLE_STELLAR_ASSET renders as stellar_asset',
      () {
        var member =
            XdrContractExecutableType.CONTRACT_EXECUTABLE_STELLAR_ASSET;
        expect(member.value, 1);
        expect(member.toXdrJsonValue(), 'stellar_asset');
        expect(member.toXdrJson(), '"stellar_asset"');
        var parsed = XdrContractExecutableType.fromXdrJson('"stellar_asset"');
        expect(parsed.value, 1);
        expect(
          parsed.toBase64EncodedXdrString(),
          member.toBase64EncodedXdrString(),
        );
        expect(
          XdrContractExecutableType.fromXdrJsonValue('stellar_asset').value,
          1,
        );
      },
    );
    test(
      'XdrContractExecutableType CONTRACT_EXECUTABLE_EXTERNAL_REF renders as external_ref',
      () {
        var member = XdrContractExecutableType.CONTRACT_EXECUTABLE_EXTERNAL_REF;
        expect(member.value, 2);
        expect(member.toXdrJsonValue(), 'external_ref');
        expect(member.toXdrJson(), '"external_ref"');
        var parsed = XdrContractExecutableType.fromXdrJson('"external_ref"');
        expect(parsed.value, 2);
        expect(
          parsed.toBase64EncodedXdrString(),
          member.toBase64EncodedXdrString(),
        );
        expect(
          XdrContractExecutableType.fromXdrJsonValue('external_ref').value,
          2,
        );
      },
    );
    test('XdrContractExecutableType rejects an undeclared member name', () {
      expect(
        () => XdrContractExecutableType.fromXdrJsonValue(
          'xdr_json_undeclared_member',
        ),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrContractExecutableType rejects an undeclared value', () {
      expect(
        () => XdrContractExecutableType(3).toXdrJsonValue(),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSCAddressType SC_ADDRESS_TYPE_ACCOUNT renders as account', () {
      var member = XdrSCAddressType.SC_ADDRESS_TYPE_ACCOUNT;
      expect(member.value, 0);
      expect(member.toXdrJsonValue(), 'account');
      expect(member.toXdrJson(), '"account"');
      var parsed = XdrSCAddressType.fromXdrJson('"account"');
      expect(parsed.value, 0);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCAddressType.fromXdrJsonValue('account').value, 0);
    });
    test('XdrSCAddressType SC_ADDRESS_TYPE_CONTRACT renders as contract', () {
      var member = XdrSCAddressType.SC_ADDRESS_TYPE_CONTRACT;
      expect(member.value, 1);
      expect(member.toXdrJsonValue(), 'contract');
      expect(member.toXdrJson(), '"contract"');
      var parsed = XdrSCAddressType.fromXdrJson('"contract"');
      expect(parsed.value, 1);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSCAddressType.fromXdrJsonValue('contract').value, 1);
    });
    test(
      'XdrSCAddressType SC_ADDRESS_TYPE_MUXED_ACCOUNT renders as muxed_account',
      () {
        var member = XdrSCAddressType.SC_ADDRESS_TYPE_MUXED_ACCOUNT;
        expect(member.value, 2);
        expect(member.toXdrJsonValue(), 'muxed_account');
        expect(member.toXdrJson(), '"muxed_account"');
        var parsed = XdrSCAddressType.fromXdrJson('"muxed_account"');
        expect(parsed.value, 2);
        expect(
          parsed.toBase64EncodedXdrString(),
          member.toBase64EncodedXdrString(),
        );
        expect(XdrSCAddressType.fromXdrJsonValue('muxed_account').value, 2);
      },
    );
    test(
      'XdrSCAddressType SC_ADDRESS_TYPE_CLAIMABLE_BALANCE renders as claimable_balance',
      () {
        var member = XdrSCAddressType.SC_ADDRESS_TYPE_CLAIMABLE_BALANCE;
        expect(member.value, 3);
        expect(member.toXdrJsonValue(), 'claimable_balance');
        expect(member.toXdrJson(), '"claimable_balance"');
        var parsed = XdrSCAddressType.fromXdrJson('"claimable_balance"');
        expect(parsed.value, 3);
        expect(
          parsed.toBase64EncodedXdrString(),
          member.toBase64EncodedXdrString(),
        );
        expect(XdrSCAddressType.fromXdrJsonValue('claimable_balance').value, 3);
      },
    );
    test(
      'XdrSCAddressType SC_ADDRESS_TYPE_LIQUIDITY_POOL renders as liquidity_pool',
      () {
        var member = XdrSCAddressType.SC_ADDRESS_TYPE_LIQUIDITY_POOL;
        expect(member.value, 4);
        expect(member.toXdrJsonValue(), 'liquidity_pool');
        expect(member.toXdrJson(), '"liquidity_pool"');
        var parsed = XdrSCAddressType.fromXdrJson('"liquidity_pool"');
        expect(parsed.value, 4);
        expect(
          parsed.toBase64EncodedXdrString(),
          member.toBase64EncodedXdrString(),
        );
        expect(XdrSCAddressType.fromXdrJsonValue('liquidity_pool').value, 4);
      },
    );
    test('XdrSCAddressType rejects an undeclared member name', () {
      expect(
        () => XdrSCAddressType.fromXdrJsonValue('xdr_json_undeclared_member'),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSCAddressType rejects an undeclared value', () {
      expect(
        () => XdrSCAddressType(5).toXdrJsonValue(),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrMuxedAccountMed25519 round trip', () {
      var original = XdrMuxedAccountMed25519(
        XdrUint64(BigInt.from(123456)),
        XdrUint256(Uint8List.fromList(List<int>.filled(32, 0xAB))),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrMuxedAccountMed25519.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrMuxedAccountMed25519.fromXdrJsonValue(
        original.toXdrJsonValue(),
      );
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test(
      'XdrMuxedAccountMed25519 rejects a rendering that is not a string',
      () {
        expect(
          () => XdrMuxedAccountMed25519.fromXdrJsonValue(<String, Object?>{}),
          throwsA(isA<FormatException>()),
        );
      },
    );
    test('XdrSCAddress account arm round trip', () {
      var original = XdrSCAddress(XdrSCAddressType.SC_ADDRESS_TYPE_ACCOUNT);
      original.accountId = (XdrAccountID(
        XdrPublicKey(
          XdrPublicKeyType.PUBLIC_KEY_TYPE_ED25519,
        )..ed25519 = XdrUint256(Uint8List.fromList(List<int>.filled(32, 0xAB))),
      ));
      var json = original.toXdrJson();
      var fromDocument = XdrSCAddress.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCAddress.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCAddress contract arm round trip', () {
      var original = XdrSCAddress(XdrSCAddressType.SC_ADDRESS_TYPE_CONTRACT);
      original.contractId = XdrHash(
        Uint8List.fromList(List<int>.filled(32, 0xAB)),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCAddress.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCAddress.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCAddress muxed_account arm round trip', () {
      var original = XdrSCAddress(
        XdrSCAddressType.SC_ADDRESS_TYPE_MUXED_ACCOUNT,
      );
      original.muxedAccount = XdrMuxedAccountMed25519(
        XdrUint64(BigInt.from(123456)),
        XdrUint256(Uint8List.fromList(List<int>.filled(32, 0xAB))),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCAddress.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCAddress.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCAddress claimable_balance arm round trip', () {
      var original = XdrSCAddress(
        XdrSCAddressType.SC_ADDRESS_TYPE_CLAIMABLE_BALANCE,
      );
      original.claimableBalanceId = (XdrClaimableBalanceID(
        XdrClaimableBalanceIDType.CLAIMABLE_BALANCE_ID_TYPE_V0,
      )..v0 = XdrHash(Uint8List.fromList(List<int>.filled(32, 0xAB))));
      var json = original.toXdrJson();
      var fromDocument = XdrSCAddress.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCAddress.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCAddress liquidity_pool arm round trip', () {
      var original = XdrSCAddress(
        XdrSCAddressType.SC_ADDRESS_TYPE_LIQUIDITY_POOL,
      );
      original.liquidityPoolId = XdrHash(
        Uint8List.fromList(List<int>.filled(32, 0xAB)),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCAddress.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCAddress.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCAddress rejects an undeclared discriminant', () {
      expect(
        () => XdrSCAddress(XdrSCAddressType(5)).toXdrJsonValue(),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSCAddress rejects a rendering that is not a string', () {
      expect(
        () => XdrSCAddress.fromXdrJsonValue(<String, Object?>{}),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrContractExecutableExternalRef round trip', () {
      var original = XdrContractExecutableExternalRef(
        (XdrSCAddress(XdrSCAddressType.SC_ADDRESS_TYPE_ACCOUNT)
          ..accountId = XdrAccountID(
            (XdrPublicKey(XdrPublicKeyType.PUBLIC_KEY_TYPE_ED25519)
              ..ed25519 = XdrUint256(
                Uint8List.fromList(List<int>.filled(32, 0xAB)),
              )),
          )),
        Uint8List.fromList([1, 2, 3, 4]),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrContractExecutableExternalRef.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrContractExecutableExternalRef.fromXdrJsonValue(
        original.toXdrJsonValue(),
      );
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrContractExecutableExternalRef rejects an undeclared key', () {
      var original = XdrContractExecutableExternalRef(
        (XdrSCAddress(XdrSCAddressType.SC_ADDRESS_TYPE_ACCOUNT)
          ..accountId = XdrAccountID(
            (XdrPublicKey(XdrPublicKeyType.PUBLIC_KEY_TYPE_ED25519)
              ..ed25519 = XdrUint256(
                Uint8List.fromList(List<int>.filled(32, 0xAB)),
              )),
          )),
        Uint8List.fromList([1, 2, 3, 4]),
      );
      var object = <String, Object?>{
        ...(original.toXdrJsonValue() as Map<String, Object?>),
        'xdr_json_undeclared_key': null,
      };
      expect(
        () => XdrContractExecutableExternalRef.fromXdrJsonValue(object),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrContractExecutable wasm arm round trip', () {
      var original = XdrContractExecutable(
        XdrContractExecutableType.CONTRACT_EXECUTABLE_WASM,
      );
      original.wasmHash = XdrHash(
        Uint8List.fromList(List<int>.filled(32, 0xAB)),
      );
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'wasm',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrContractExecutable.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrContractExecutable.fromXdrJsonValue(
        original.toXdrJsonValue(),
      );
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrContractExecutable stellar_asset arm round trip', () {
      var original = XdrContractExecutable(
        XdrContractExecutableType.CONTRACT_EXECUTABLE_STELLAR_ASSET,
      );
      expect(original.toXdrJsonValue(), 'stellar_asset');
      var json = original.toXdrJson();
      var fromDocument = XdrContractExecutable.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrContractExecutable.fromXdrJsonValue(
        original.toXdrJsonValue(),
      );
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrContractExecutable external_ref arm round trip', () {
      var original = XdrContractExecutable(
        XdrContractExecutableType.CONTRACT_EXECUTABLE_EXTERNAL_REF,
      );
      original.externalRef = (XdrContractExecutableExternalRef(
        (XdrSCAddress(XdrSCAddressType.SC_ADDRESS_TYPE_ACCOUNT)
          ..accountId = XdrAccountID(
            (XdrPublicKey(XdrPublicKeyType.PUBLIC_KEY_TYPE_ED25519)
              ..ed25519 = XdrUint256(
                Uint8List.fromList(List<int>.filled(32, 0xAB)),
              )),
          )),
        Uint8List.fromList([1, 2, 3, 4]),
      ));
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'external_ref',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrContractExecutable.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrContractExecutable.fromXdrJsonValue(
        original.toXdrJsonValue(),
      );
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrContractExecutable rejects an undeclared discriminant', () {
      expect(
        () => XdrContractExecutable(
          XdrContractExecutableType(3),
        ).toXdrJsonValue(),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrContractExecutable rejects an undeclared arm name', () {
      expect(
        () => XdrContractExecutable.fromXdrJsonValue('xdr_json_undeclared_arm'),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrContractExecutable rejects an undeclared arm key', () {
      expect(
        () => XdrContractExecutable.fromXdrJsonValue(<String, Object?>{
          'xdr_json_undeclared_arm': null,
        }),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSCNonceKey round trip', () {
      var original = XdrSCNonceKey(XdrInt64(BigInt.from(654321)));
      var json = original.toXdrJson();
      var fromDocument = XdrSCNonceKey.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCNonceKey.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCNonceKey rejects an undeclared key', () {
      var original = XdrSCNonceKey(XdrInt64(BigInt.from(654321)));
      var object = <String, Object?>{
        ...(original.toXdrJsonValue() as Map<String, Object?>),
        'xdr_json_undeclared_key': null,
      };
      expect(
        () => XdrSCNonceKey.fromXdrJsonValue(object),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSCContractInstance round trip', () {
      var original = XdrSCContractInstance(
        XdrContractExecutable(
          XdrContractExecutableType.CONTRACT_EXECUTABLE_STELLAR_ASSET,
        ),
        null,
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCContractInstance.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCContractInstance.fromXdrJsonValue(
        original.toXdrJsonValue(),
      );
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test(
      'XdrSCContractInstance round trip with its optional fields present',
      () {
        var original = XdrSCContractInstance(
          XdrContractExecutable(
            XdrContractExecutableType.CONTRACT_EXECUTABLE_STELLAR_ASSET,
          ),
          null,
        );
        original.storage = [
          XdrSCMapEntry(
            XdrSCVal(XdrSCValType.SCV_VOID),
            XdrSCVal(XdrSCValType.SCV_VOID),
          ),
        ];
        var json = original.toXdrJson();
        var fromDocument = XdrSCContractInstance.fromXdrJson(json);
        expect(fromDocument.toXdrJson(), json);
        expect(
          fromDocument.toBase64EncodedXdrString(),
          original.toBase64EncodedXdrString(),
        );
        var fromTree = XdrSCContractInstance.fromXdrJsonValue(
          original.toXdrJsonValue(),
        );
        expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
        expect(
          fromTree.toBase64EncodedXdrString(),
          original.toBase64EncodedXdrString(),
        );
      },
    );
    test('XdrSCContractInstance rejects an undeclared key', () {
      var original = XdrSCContractInstance(
        XdrContractExecutable(
          XdrContractExecutableType.CONTRACT_EXECUTABLE_STELLAR_ASSET,
        ),
        null,
      );
      var object = <String, Object?>{
        ...(original.toXdrJsonValue() as Map<String, Object?>),
        'xdr_json_undeclared_key': null,
      };
      expect(
        () => XdrSCContractInstance.fromXdrJsonValue(object),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSCVal bool arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_BOOL);
      original.b = true;
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'bool',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal void arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_VOID);
      expect(original.toXdrJsonValue(), 'void');
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal error arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_ERROR);
      original.error = (XdrSCError(XdrSCErrorType.SCE_CONTRACT)
        ..contractCode = XdrUint32(42));
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'error',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal u32 arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_U32);
      original.u32 = XdrUint32(42);
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'u32',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal i32 arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_I32);
      original.i32 = XdrInt32(7);
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'i32',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal u64 arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_U64);
      original.u64 = XdrUint64(BigInt.from(123456));
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'u64',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal i64 arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_I64);
      original.i64 = XdrInt64(BigInt.from(654321));
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'i64',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal timepoint arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_TIMEPOINT);
      original.timepoint = XdrUint64(BigInt.from(123456));
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'timepoint',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal duration arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_DURATION);
      original.duration = XdrUint64(BigInt.from(123456));
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'duration',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal u128 arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_U128);
      original.u128 = XdrUInt128Parts(
        XdrUint64(BigInt.from(123456)),
        XdrUint64(BigInt.from(123456)),
      );
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'u128',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal i128 arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_I128);
      original.i128 = XdrInt128Parts(
        XdrInt64(BigInt.from(654321)),
        XdrUint64(BigInt.from(123456)),
      );
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'i128',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal u256 arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_U256);
      original.u256 = XdrUInt256Parts(
        XdrUint64(BigInt.from(123456)),
        XdrUint64(BigInt.from(123456)),
        XdrUint64(BigInt.from(123456)),
        XdrUint64(BigInt.from(123456)),
      );
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'u256',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal i256 arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_I256);
      original.i256 = XdrInt256Parts(
        XdrInt64(BigInt.from(654321)),
        XdrUint64(BigInt.from(123456)),
        XdrUint64(BigInt.from(123456)),
        XdrUint64(BigInt.from(123456)),
      );
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'i256',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal bytes arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_BYTES);
      original.bytes = XdrSCBytes(Uint8List.fromList([1, 2, 3]));
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'bytes',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal string arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_STRING);
      original.str = 'test_string';
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'string',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal symbol arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_SYMBOL);
      original.sym = 'test_string';
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'symbol',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal vec arm round trip with an absent body', () {
      var original = XdrSCVal(XdrSCValType.SCV_VEC);
      original.vec = null;
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'vec',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal vec arm round trip with an empty body', () {
      var original = XdrSCVal(XdrSCValType.SCV_VEC);
      original.vec = <XdrSCVal>[];
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'vec',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal vec arm round trip with a present body', () {
      var original = XdrSCVal(XdrSCValType.SCV_VEC);
      original.vec = [XdrSCVal(XdrSCValType.SCV_VOID)];
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'vec',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal map arm round trip with an absent body', () {
      var original = XdrSCVal(XdrSCValType.SCV_MAP);
      original.map = null;
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'map',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal map arm round trip with an empty body', () {
      var original = XdrSCVal(XdrSCValType.SCV_MAP);
      original.map = <XdrSCMapEntry>[];
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'map',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal map arm round trip with a present body', () {
      var original = XdrSCVal(XdrSCValType.SCV_MAP);
      original.map = [
        XdrSCMapEntry(
          XdrSCVal(XdrSCValType.SCV_VOID),
          XdrSCVal(XdrSCValType.SCV_VOID),
        ),
      ];
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'map',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal address arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_ADDRESS);
      original.address = (XdrSCAddress(XdrSCAddressType.SC_ADDRESS_TYPE_ACCOUNT)
        ..accountId = XdrAccountID(
          (XdrPublicKey(XdrPublicKeyType.PUBLIC_KEY_TYPE_ED25519)
            ..ed25519 = XdrUint256(
              Uint8List.fromList(List<int>.filled(32, 0xAB)),
            )),
        ));
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'address',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal contract_instance arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_CONTRACT_INSTANCE);
      original.instance = XdrSCContractInstance(
        XdrContractExecutable(
          XdrContractExecutableType.CONTRACT_EXECUTABLE_STELLAR_ASSET,
        ),
        null,
      );
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'contract_instance',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal ledger_key_contract_instance arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_LEDGER_KEY_CONTRACT_INSTANCE);
      expect(original.toXdrJsonValue(), 'ledger_key_contract_instance');
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal ledger_key_nonce arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_LEDGER_KEY_NONCE);
      original.nonce_key = XdrSCNonceKey(XdrInt64(BigInt.from(654321)));
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'ledger_key_nonce',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal executable_tag arm round trip', () {
      var original = XdrSCVal(XdrSCValType.SCV_EXECUTABLE_TAG);
      original.executableTag = Uint8List.fromList([1, 2, 3, 4]);
      expect(
        (original.toXdrJsonValue() as Map<String, Object?>).keys.single,
        'executable_tag',
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCVal.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCVal.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCVal rejects an undeclared discriminant', () {
      expect(
        () => XdrSCVal(XdrSCValType(23)).toXdrJsonValue(),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSCVal rejects an undeclared arm name', () {
      expect(
        () => XdrSCVal.fromXdrJsonValue('xdr_json_undeclared_arm'),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSCVal rejects an undeclared arm key', () {
      expect(
        () => XdrSCVal.fromXdrJsonValue(<String, Object?>{
          'xdr_json_undeclared_arm': null,
        }),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSCMapEntry round trip', () {
      var original = XdrSCMapEntry(
        XdrSCVal(XdrSCValType.SCV_VOID),
        XdrSCVal(XdrSCValType.SCV_VOID),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSCMapEntry.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSCMapEntry.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSCMapEntry rejects an undeclared key', () {
      var original = XdrSCMapEntry(
        XdrSCVal(XdrSCValType.SCV_VOID),
        XdrSCVal(XdrSCValType.SCV_VOID),
      );
      var object = <String, Object?>{
        ...(original.toXdrJsonValue() as Map<String, Object?>),
        'xdr_json_undeclared_key': null,
      };
      expect(
        () => XdrSCMapEntry.fromXdrJsonValue(object),
        throwsA(isA<FormatException>()),
      );
    });
  });
}
