// AUTO-GENERATED - DO NOT EDIT
// Generated by tools/xdr-generator/test/generate_json_tests.rb

import 'dart:typed_data';
import 'package:flutter_test/flutter_test.dart';
import 'package:stellar_flutter_sdk/stellar_flutter_sdk.dart';

void main() {
  group('SEP-0051 types renderings', () {
    test('XdrHash round trip', () {
      var original = XdrHash(Uint8List.fromList(List<int>.filled(32, 0xAB)));
      var json = original.toXdrJson();
      var fromDocument = XdrHash.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrHash.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrUint256 round trip', () {
      var original = XdrUint256(Uint8List.fromList(List<int>.filled(32, 0xAB)));
      var json = original.toXdrJson();
      var fromDocument = XdrUint256.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrUint256.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrUint32 round trip', () {
      var original = XdrUint32(42);
      var json = original.toXdrJson();
      var fromDocument = XdrUint32.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrUint32.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrInt32 round trip', () {
      var original = XdrInt32(42);
      var json = original.toXdrJson();
      var fromDocument = XdrInt32.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrInt32.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrUint64 round trip', () {
      var original = XdrUint64(BigInt.from(123456789));
      var json = original.toXdrJson();
      var fromDocument = XdrUint64.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrUint64.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrInt64 round trip', () {
      var original = XdrInt64(BigInt.from(123456789));
      var json = original.toXdrJson();
      var fromDocument = XdrInt64.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrInt64.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrExtensionPoint v0 arm round trip', () {
      var original = XdrExtensionPoint(0);
      expect(original.toXdrJsonValue(), 'v0');
      var json = original.toXdrJson();
      var fromDocument = XdrExtensionPoint.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrExtensionPoint.fromXdrJsonValue(
        original.toXdrJsonValue(),
      );
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrExtensionPoint rejects an undeclared discriminant', () {
      expect(
        () => XdrExtensionPoint(1).toXdrJsonValue(),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrExtensionPoint rejects an undeclared arm name', () {
      expect(
        () => XdrExtensionPoint.fromXdrJsonValue('xdr_json_undeclared_arm'),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrExtensionPoint rejects a value that is not an arm name', () {
      expect(
        () => XdrExtensionPoint.fromXdrJsonValue(<String, Object?>{}),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrCryptoKeyType KEY_TYPE_ED25519 renders as ed25519', () {
      var member = XdrCryptoKeyType.KEY_TYPE_ED25519;
      expect(member.value, 0);
      expect(member.toXdrJsonValue(), 'ed25519');
      expect(member.toXdrJson(), '"ed25519"');
      var parsed = XdrCryptoKeyType.fromXdrJson('"ed25519"');
      expect(parsed.value, 0);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrCryptoKeyType.fromXdrJsonValue('ed25519').value, 0);
    });
    test('XdrCryptoKeyType KEY_TYPE_PRE_AUTH_TX renders as pre_auth_tx', () {
      var member = XdrCryptoKeyType.KEY_TYPE_PRE_AUTH_TX;
      expect(member.value, 1);
      expect(member.toXdrJsonValue(), 'pre_auth_tx');
      expect(member.toXdrJson(), '"pre_auth_tx"');
      var parsed = XdrCryptoKeyType.fromXdrJson('"pre_auth_tx"');
      expect(parsed.value, 1);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrCryptoKeyType.fromXdrJsonValue('pre_auth_tx').value, 1);
    });
    test('XdrCryptoKeyType KEY_TYPE_HASH_X renders as hash_x', () {
      var member = XdrCryptoKeyType.KEY_TYPE_HASH_X;
      expect(member.value, 2);
      expect(member.toXdrJsonValue(), 'hash_x');
      expect(member.toXdrJson(), '"hash_x"');
      var parsed = XdrCryptoKeyType.fromXdrJson('"hash_x"');
      expect(parsed.value, 2);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrCryptoKeyType.fromXdrJsonValue('hash_x').value, 2);
    });
    test(
      'XdrCryptoKeyType KEY_TYPE_ED25519_SIGNED_PAYLOAD renders as ed25519_signed_payload',
      () {
        var member = XdrCryptoKeyType.KEY_TYPE_ED25519_SIGNED_PAYLOAD;
        expect(member.value, 3);
        expect(member.toXdrJsonValue(), 'ed25519_signed_payload');
        expect(member.toXdrJson(), '"ed25519_signed_payload"');
        var parsed = XdrCryptoKeyType.fromXdrJson('"ed25519_signed_payload"');
        expect(parsed.value, 3);
        expect(
          parsed.toBase64EncodedXdrString(),
          member.toBase64EncodedXdrString(),
        );
        expect(
          XdrCryptoKeyType.fromXdrJsonValue('ed25519_signed_payload').value,
          3,
        );
      },
    );
    test(
      'XdrCryptoKeyType KEY_TYPE_MUXED_ED25519 renders as muxed_ed25519',
      () {
        var member = XdrCryptoKeyType.KEY_TYPE_MUXED_ED25519;
        expect(member.value, 256);
        expect(member.toXdrJsonValue(), 'muxed_ed25519');
        expect(member.toXdrJson(), '"muxed_ed25519"');
        var parsed = XdrCryptoKeyType.fromXdrJson('"muxed_ed25519"');
        expect(parsed.value, 256);
        expect(
          parsed.toBase64EncodedXdrString(),
          member.toBase64EncodedXdrString(),
        );
        expect(XdrCryptoKeyType.fromXdrJsonValue('muxed_ed25519').value, 256);
      },
    );
    test('XdrCryptoKeyType rejects an undeclared member name', () {
      expect(
        () => XdrCryptoKeyType.fromXdrJsonValue('xdr_json_undeclared_member'),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrCryptoKeyType rejects an undeclared value', () {
      expect(
        () => XdrCryptoKeyType(4).toXdrJsonValue(),
        throwsA(isA<FormatException>()),
      );
    });
    test(
      'XdrPublicKeyType PUBLIC_KEY_TYPE_ED25519 renders as public_key_type_ed25519',
      () {
        var member = XdrPublicKeyType.PUBLIC_KEY_TYPE_ED25519;
        expect(member.value, 0);
        expect(member.toXdrJsonValue(), 'public_key_type_ed25519');
        expect(member.toXdrJson(), '"public_key_type_ed25519"');
        var parsed = XdrPublicKeyType.fromXdrJson('"public_key_type_ed25519"');
        expect(parsed.value, 0);
        expect(
          parsed.toBase64EncodedXdrString(),
          member.toBase64EncodedXdrString(),
        );
        expect(
          XdrPublicKeyType.fromXdrJsonValue('public_key_type_ed25519').value,
          0,
        );
      },
    );
    test('XdrPublicKeyType rejects an undeclared member name', () {
      expect(
        () => XdrPublicKeyType.fromXdrJsonValue('xdr_json_undeclared_member'),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrPublicKeyType rejects an undeclared value', () {
      expect(
        () => XdrPublicKeyType(1).toXdrJsonValue(),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSignerKeyType SIGNER_KEY_TYPE_ED25519 renders as ed25519', () {
      var member = XdrSignerKeyType.SIGNER_KEY_TYPE_ED25519;
      expect(member.value, 0);
      expect(member.toXdrJsonValue(), 'ed25519');
      expect(member.toXdrJson(), '"ed25519"');
      var parsed = XdrSignerKeyType.fromXdrJson('"ed25519"');
      expect(parsed.value, 0);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSignerKeyType.fromXdrJsonValue('ed25519').value, 0);
    });
    test(
      'XdrSignerKeyType SIGNER_KEY_TYPE_PRE_AUTH_TX renders as pre_auth_tx',
      () {
        var member = XdrSignerKeyType.SIGNER_KEY_TYPE_PRE_AUTH_TX;
        expect(member.value, 1);
        expect(member.toXdrJsonValue(), 'pre_auth_tx');
        expect(member.toXdrJson(), '"pre_auth_tx"');
        var parsed = XdrSignerKeyType.fromXdrJson('"pre_auth_tx"');
        expect(parsed.value, 1);
        expect(
          parsed.toBase64EncodedXdrString(),
          member.toBase64EncodedXdrString(),
        );
        expect(XdrSignerKeyType.fromXdrJsonValue('pre_auth_tx').value, 1);
      },
    );
    test('XdrSignerKeyType SIGNER_KEY_TYPE_HASH_X renders as hash_x', () {
      var member = XdrSignerKeyType.SIGNER_KEY_TYPE_HASH_X;
      expect(member.value, 2);
      expect(member.toXdrJsonValue(), 'hash_x');
      expect(member.toXdrJson(), '"hash_x"');
      var parsed = XdrSignerKeyType.fromXdrJson('"hash_x"');
      expect(parsed.value, 2);
      expect(
        parsed.toBase64EncodedXdrString(),
        member.toBase64EncodedXdrString(),
      );
      expect(XdrSignerKeyType.fromXdrJsonValue('hash_x').value, 2);
    });
    test(
      'XdrSignerKeyType SIGNER_KEY_TYPE_ED25519_SIGNED_PAYLOAD renders as ed25519_signed_payload',
      () {
        var member = XdrSignerKeyType.SIGNER_KEY_TYPE_ED25519_SIGNED_PAYLOAD;
        expect(member.value, 3);
        expect(member.toXdrJsonValue(), 'ed25519_signed_payload');
        expect(member.toXdrJson(), '"ed25519_signed_payload"');
        var parsed = XdrSignerKeyType.fromXdrJson('"ed25519_signed_payload"');
        expect(parsed.value, 3);
        expect(
          parsed.toBase64EncodedXdrString(),
          member.toBase64EncodedXdrString(),
        );
        expect(
          XdrSignerKeyType.fromXdrJsonValue('ed25519_signed_payload').value,
          3,
        );
      },
    );
    test('XdrSignerKeyType rejects an undeclared member name', () {
      expect(
        () => XdrSignerKeyType.fromXdrJsonValue('xdr_json_undeclared_member'),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSignerKeyType rejects an undeclared value', () {
      expect(
        () => XdrSignerKeyType(4).toXdrJsonValue(),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrPublicKey public_key_type_ed25519 arm round trip', () {
      var original = XdrPublicKey(XdrPublicKeyType.PUBLIC_KEY_TYPE_ED25519);
      original.ed25519 = XdrUint256(
        Uint8List.fromList(List<int>.filled(32, 0xAB)),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrPublicKey.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrPublicKey.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrPublicKey rejects an undeclared discriminant', () {
      expect(
        () => XdrPublicKey(XdrPublicKeyType(1)).toXdrJsonValue(),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrPublicKey rejects a rendering that is not a string', () {
      expect(
        () => XdrPublicKey.fromXdrJsonValue(<String, Object?>{}),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSignedPayload round trip', () {
      var original = XdrSignedPayload(
        XdrUint256(Uint8List.fromList(List<int>.filled(32, 0xAB))),
        XdrDataValue(Uint8List.fromList([1, 2, 3])),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSignedPayload.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSignedPayload.fromXdrJsonValue(
        original.toXdrJsonValue(),
      );
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSignedPayload rejects a rendering that is not a string', () {
      expect(
        () => XdrSignedPayload.fromXdrJsonValue(<String, Object?>{}),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSignerKey ed25519 arm round trip', () {
      var original = XdrSignerKey(XdrSignerKeyType.SIGNER_KEY_TYPE_ED25519);
      original.ed25519 = XdrUint256(
        Uint8List.fromList(List<int>.filled(32, 0xAB)),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSignerKey.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSignerKey.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSignerKey pre_auth_tx arm round trip', () {
      var original = XdrSignerKey(XdrSignerKeyType.SIGNER_KEY_TYPE_PRE_AUTH_TX);
      original.preAuthTx = XdrUint256(
        Uint8List.fromList(List<int>.filled(32, 0xAB)),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSignerKey.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSignerKey.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSignerKey hash_x arm round trip', () {
      var original = XdrSignerKey(XdrSignerKeyType.SIGNER_KEY_TYPE_HASH_X);
      original.hashX = XdrUint256(
        Uint8List.fromList(List<int>.filled(32, 0xAB)),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSignerKey.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSignerKey.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSignerKey ed25519_signed_payload arm round trip', () {
      var original = XdrSignerKey(
        XdrSignerKeyType.SIGNER_KEY_TYPE_ED25519_SIGNED_PAYLOAD,
      );
      original.signedPayload = XdrSignedPayload(
        XdrUint256(Uint8List.fromList(List<int>.filled(32, 0xAB))),
        XdrDataValue(Uint8List.fromList([1, 2, 3])),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSignerKey.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSignerKey.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSignerKey rejects an undeclared discriminant', () {
      expect(
        () => XdrSignerKey(XdrSignerKeyType(4)).toXdrJsonValue(),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSignerKey rejects a rendering that is not a string', () {
      expect(
        () => XdrSignerKey.fromXdrJsonValue(<String, Object?>{}),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSignature round trip', () {
      var original = XdrSignature(Uint8List.fromList([1, 2, 3]));
      var json = original.toXdrJson();
      var fromDocument = XdrSignature.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSignature.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSignatureHint round trip', () {
      var original = XdrSignatureHint(
        Uint8List.fromList(List<int>.filled(4, 0xAB)),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSignatureHint.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSignatureHint.fromXdrJsonValue(
        original.toXdrJsonValue(),
      );
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrNodeID round trip', () {
      var original = XdrNodeID(
        (XdrPublicKey(XdrPublicKeyType.PUBLIC_KEY_TYPE_ED25519)
          ..ed25519 = XdrUint256(
            Uint8List.fromList(List<int>.filled(32, 0xAB)),
          )),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrNodeID.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrNodeID.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrAccountID round trip', () {
      var original = XdrAccountID(
        (XdrPublicKey(XdrPublicKeyType.PUBLIC_KEY_TYPE_ED25519)
          ..ed25519 = XdrUint256(
            Uint8List.fromList(List<int>.filled(32, 0xAB)),
          )),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrAccountID.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrAccountID.fromXdrJsonValue(original.toXdrJsonValue());
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrCurve25519Secret round trip', () {
      var original = XdrCurve25519Secret(
        Uint8List.fromList(List<int>.filled(32, 0xAB)),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrCurve25519Secret.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrCurve25519Secret.fromXdrJsonValue(
        original.toXdrJsonValue(),
      );
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrCurve25519Secret rejects an undeclared key', () {
      var original = XdrCurve25519Secret(
        Uint8List.fromList(List<int>.filled(32, 0xAB)),
      );
      var object = <String, Object?>{
        ...(original.toXdrJsonValue() as Map<String, Object?>),
        'xdr_json_undeclared_key': null,
      };
      expect(
        () => XdrCurve25519Secret.fromXdrJsonValue(object),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrCurve25519Public round trip', () {
      var original = XdrCurve25519Public(
        Uint8List.fromList(List<int>.filled(32, 0xAB)),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrCurve25519Public.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrCurve25519Public.fromXdrJsonValue(
        original.toXdrJsonValue(),
      );
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrCurve25519Public rejects an undeclared key', () {
      var original = XdrCurve25519Public(
        Uint8List.fromList(List<int>.filled(32, 0xAB)),
      );
      var object = <String, Object?>{
        ...(original.toXdrJsonValue() as Map<String, Object?>),
        'xdr_json_undeclared_key': null,
      };
      expect(
        () => XdrCurve25519Public.fromXdrJsonValue(object),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrHmacSha256Key round trip', () {
      var original = XdrHmacSha256Key(
        Uint8List.fromList(List<int>.filled(32, 0xAB)),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrHmacSha256Key.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrHmacSha256Key.fromXdrJsonValue(
        original.toXdrJsonValue(),
      );
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrHmacSha256Key rejects an undeclared key', () {
      var original = XdrHmacSha256Key(
        Uint8List.fromList(List<int>.filled(32, 0xAB)),
      );
      var object = <String, Object?>{
        ...(original.toXdrJsonValue() as Map<String, Object?>),
        'xdr_json_undeclared_key': null,
      };
      expect(
        () => XdrHmacSha256Key.fromXdrJsonValue(object),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrHmacSha256Mac round trip', () {
      var original = XdrHmacSha256Mac(
        Uint8List.fromList(List<int>.filled(32, 0xAB)),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrHmacSha256Mac.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrHmacSha256Mac.fromXdrJsonValue(
        original.toXdrJsonValue(),
      );
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrHmacSha256Mac rejects an undeclared key', () {
      var original = XdrHmacSha256Mac(
        Uint8List.fromList(List<int>.filled(32, 0xAB)),
      );
      var object = <String, Object?>{
        ...(original.toXdrJsonValue() as Map<String, Object?>),
        'xdr_json_undeclared_key': null,
      };
      expect(
        () => XdrHmacSha256Mac.fromXdrJsonValue(object),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrShortHashSeed round trip', () {
      var original = XdrShortHashSeed(
        Uint8List.fromList(List<int>.filled(16, 0xAB)),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrShortHashSeed.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrShortHashSeed.fromXdrJsonValue(
        original.toXdrJsonValue(),
      );
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrShortHashSeed rejects an undeclared key', () {
      var original = XdrShortHashSeed(
        Uint8List.fromList(List<int>.filled(16, 0xAB)),
      );
      var object = <String, Object?>{
        ...(original.toXdrJsonValue() as Map<String, Object?>),
        'xdr_json_undeclared_key': null,
      };
      expect(
        () => XdrShortHashSeed.fromXdrJsonValue(object),
        throwsA(isA<FormatException>()),
      );
    });
    test(
      'XdrBinaryFuseFilterType BINARY_FUSE_FILTER_8_BIT renders as b8_bit',
      () {
        var member = XdrBinaryFuseFilterType.BINARY_FUSE_FILTER_8_BIT;
        expect(member.value, 0);
        expect(member.toXdrJsonValue(), 'b8_bit');
        expect(member.toXdrJson(), '"b8_bit"');
        var parsed = XdrBinaryFuseFilterType.fromXdrJson('"b8_bit"');
        expect(parsed.value, 0);
        expect(
          parsed.toBase64EncodedXdrString(),
          member.toBase64EncodedXdrString(),
        );
        expect(XdrBinaryFuseFilterType.fromXdrJsonValue('b8_bit').value, 0);
      },
    );
    test(
      'XdrBinaryFuseFilterType BINARY_FUSE_FILTER_16_BIT renders as b16_bit',
      () {
        var member = XdrBinaryFuseFilterType.BINARY_FUSE_FILTER_16_BIT;
        expect(member.value, 1);
        expect(member.toXdrJsonValue(), 'b16_bit');
        expect(member.toXdrJson(), '"b16_bit"');
        var parsed = XdrBinaryFuseFilterType.fromXdrJson('"b16_bit"');
        expect(parsed.value, 1);
        expect(
          parsed.toBase64EncodedXdrString(),
          member.toBase64EncodedXdrString(),
        );
        expect(XdrBinaryFuseFilterType.fromXdrJsonValue('b16_bit').value, 1);
      },
    );
    test(
      'XdrBinaryFuseFilterType BINARY_FUSE_FILTER_32_BIT renders as b32_bit',
      () {
        var member = XdrBinaryFuseFilterType.BINARY_FUSE_FILTER_32_BIT;
        expect(member.value, 2);
        expect(member.toXdrJsonValue(), 'b32_bit');
        expect(member.toXdrJson(), '"b32_bit"');
        var parsed = XdrBinaryFuseFilterType.fromXdrJson('"b32_bit"');
        expect(parsed.value, 2);
        expect(
          parsed.toBase64EncodedXdrString(),
          member.toBase64EncodedXdrString(),
        );
        expect(XdrBinaryFuseFilterType.fromXdrJsonValue('b32_bit').value, 2);
      },
    );
    test('XdrBinaryFuseFilterType rejects an undeclared member name', () {
      expect(
        () => XdrBinaryFuseFilterType.fromXdrJsonValue(
          'xdr_json_undeclared_member',
        ),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrBinaryFuseFilterType rejects an undeclared value', () {
      expect(
        () => XdrBinaryFuseFilterType(3).toXdrJsonValue(),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrSerializedBinaryFuseFilter round trip', () {
      var original = XdrSerializedBinaryFuseFilter(
        XdrBinaryFuseFilterType.BINARY_FUSE_FILTER_8_BIT,
        XdrShortHashSeed(Uint8List.fromList(List<int>.filled(16, 0xAB))),
        XdrShortHashSeed(Uint8List.fromList(List<int>.filled(16, 0xAB))),
        XdrUint32(42),
        XdrUint32(42),
        XdrUint32(42),
        XdrUint32(42),
        XdrUint32(42),
        Uint8List.fromList([1, 2, 3, 4]),
      );
      var json = original.toXdrJson();
      var fromDocument = XdrSerializedBinaryFuseFilter.fromXdrJson(json);
      expect(fromDocument.toXdrJson(), json);
      expect(
        fromDocument.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
      var fromTree = XdrSerializedBinaryFuseFilter.fromXdrJsonValue(
        original.toXdrJsonValue(),
      );
      expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
      expect(
        fromTree.toBase64EncodedXdrString(),
        original.toBase64EncodedXdrString(),
      );
    });
    test('XdrSerializedBinaryFuseFilter rejects an undeclared key', () {
      var original = XdrSerializedBinaryFuseFilter(
        XdrBinaryFuseFilterType.BINARY_FUSE_FILTER_8_BIT,
        XdrShortHashSeed(Uint8List.fromList(List<int>.filled(16, 0xAB))),
        XdrShortHashSeed(Uint8List.fromList(List<int>.filled(16, 0xAB))),
        XdrUint32(42),
        XdrUint32(42),
        XdrUint32(42),
        XdrUint32(42),
        XdrUint32(42),
        Uint8List.fromList([1, 2, 3, 4]),
      );
      var object = <String, Object?>{
        ...(original.toXdrJsonValue() as Map<String, Object?>),
        'xdr_json_undeclared_key': null,
      };
      expect(
        () => XdrSerializedBinaryFuseFilter.fromXdrJsonValue(object),
        throwsA(isA<FormatException>()),
      );
    });
    test(
      'XdrClaimableBalanceIDType CLAIMABLE_BALANCE_ID_TYPE_V0 renders as claimable_balance_id_type_v0',
      () {
        var member = XdrClaimableBalanceIDType.CLAIMABLE_BALANCE_ID_TYPE_V0;
        expect(member.value, 0);
        expect(member.toXdrJsonValue(), 'claimable_balance_id_type_v0');
        expect(member.toXdrJson(), '"claimable_balance_id_type_v0"');
        var parsed = XdrClaimableBalanceIDType.fromXdrJson(
          '"claimable_balance_id_type_v0"',
        );
        expect(parsed.value, 0);
        expect(
          parsed.toBase64EncodedXdrString(),
          member.toBase64EncodedXdrString(),
        );
        expect(
          XdrClaimableBalanceIDType.fromXdrJsonValue(
            'claimable_balance_id_type_v0',
          ).value,
          0,
        );
      },
    );
    test('XdrClaimableBalanceIDType rejects an undeclared member name', () {
      expect(
        () => XdrClaimableBalanceIDType.fromXdrJsonValue(
          'xdr_json_undeclared_member',
        ),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrClaimableBalanceIDType rejects an undeclared value', () {
      expect(
        () => XdrClaimableBalanceIDType(1).toXdrJsonValue(),
        throwsA(isA<FormatException>()),
      );
    });
    test(
      'XdrClaimableBalanceID claimable_balance_id_type_v0 arm round trip',
      () {
        var original = XdrClaimableBalanceID(
          XdrClaimableBalanceIDType.CLAIMABLE_BALANCE_ID_TYPE_V0,
        );
        original.v0 = XdrHash(Uint8List.fromList(List<int>.filled(32, 0xAB)));
        var json = original.toXdrJson();
        var fromDocument = XdrClaimableBalanceID.fromXdrJson(json);
        expect(fromDocument.toXdrJson(), json);
        expect(
          fromDocument.toBase64EncodedXdrString(),
          original.toBase64EncodedXdrString(),
        );
        var fromTree = XdrClaimableBalanceID.fromXdrJsonValue(
          original.toXdrJsonValue(),
        );
        expect(fromTree.toXdrJsonValue(), original.toXdrJsonValue());
        expect(
          fromTree.toBase64EncodedXdrString(),
          original.toBase64EncodedXdrString(),
        );
      },
    );
    test('XdrClaimableBalanceID rejects an undeclared discriminant', () {
      expect(
        () => XdrClaimableBalanceID(
          XdrClaimableBalanceIDType(1),
        ).toXdrJsonValue(),
        throwsA(isA<FormatException>()),
      );
    });
    test('XdrClaimableBalanceID rejects a rendering that is not a string', () {
      expect(
        () => XdrClaimableBalanceID.fromXdrJsonValue(<String, Object?>{}),
        throwsA(isA<FormatException>()),
      );
    });
  });
}
