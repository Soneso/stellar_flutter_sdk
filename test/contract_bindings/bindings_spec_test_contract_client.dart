//
// This file was generated by stellar_contract_bindings v0.5.0b0
// and stellar_sdk v13.0.0.
//
// This code requires stellar_flutter_sdk (Soneso) v3.2.2 or later.
//
// @generated
//

import 'dart:typed_data';
import 'package:stellar_flutter_sdk/stellar_flutter_sdk.dart';

/// Kind enum for BindingsSpecTestContractNone
enum BindingsSpecTestContractNoneKind {
  Elif('elif'),
  Nonlocal('nonlocal'),
  Not('not');

  final String value;

  const BindingsSpecTestContractNoneKind(this.value);

  factory BindingsSpecTestContractNoneKind.fromValue(String value) {
    return BindingsSpecTestContractNoneKind.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown BindingsSpecTestContractNoneKind value: $value'),
    );
  }
}

/// BindingsSpecTestContractNone union
class BindingsSpecTestContractNone {
  final BindingsSpecTestContractNoneKind kind;

  const BindingsSpecTestContractNone._({
    required this.kind,
  });
  factory BindingsSpecTestContractNone.elif() {
    return BindingsSpecTestContractNone._(kind: BindingsSpecTestContractNoneKind.Elif);
  }
  factory BindingsSpecTestContractNone.nonlocal() {
    return BindingsSpecTestContractNone._(kind: BindingsSpecTestContractNoneKind.Nonlocal);
  }
  factory BindingsSpecTestContractNone.not() {
    return BindingsSpecTestContractNone._(kind: BindingsSpecTestContractNoneKind.Not);
  }

  XdrSCVal toScVal() {
    switch (kind) {
      case BindingsSpecTestContractNoneKind.Elif:
        return XdrSCVal.forVec([XdrSCVal.forSymbol(kind.value)]);
      case BindingsSpecTestContractNoneKind.Nonlocal:
        return XdrSCVal.forVec([XdrSCVal.forSymbol(kind.value)]);
      case BindingsSpecTestContractNoneKind.Not:
        return XdrSCVal.forVec([XdrSCVal.forSymbol(kind.value)]);
    }
  }

  factory BindingsSpecTestContractNone.fromScVal(XdrSCVal val) {
    final vec = val.vec!;
    final kind = BindingsSpecTestContractNoneKind.fromValue(vec[0].sym!.toString());

    switch (kind) {
      case BindingsSpecTestContractNoneKind.Elif:
        return BindingsSpecTestContractNone.elif();
      case BindingsSpecTestContractNoneKind.Nonlocal:
        return BindingsSpecTestContractNone.nonlocal();
      case BindingsSpecTestContractNoneKind.Not:
        return BindingsSpecTestContractNone.not();
    }
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) return true;
    if (other is! BindingsSpecTestContractNone) return false;
    if (kind != other.kind) return false;

    switch (kind) {
      case BindingsSpecTestContractNoneKind.Elif:
        return true;
      case BindingsSpecTestContractNoneKind.Nonlocal:
        return true;
      case BindingsSpecTestContractNoneKind.Not:
        return true;
    }
  }

  @override
  int get hashCode {
    switch (kind) {
      case BindingsSpecTestContractNoneKind.Elif:
        return kind.hashCode;
      case BindingsSpecTestContractNoneKind.Nonlocal:
        return kind.hashCode;
      case BindingsSpecTestContractNoneKind.Not:
        return kind.hashCode;
    }
  }
}

/// This is from the rust doc above the struct SimpleStruct
class BindingsSpecTestContractTrue {
  final int def;

  const BindingsSpecTestContractTrue({
    required this.def,
  });

  XdrSCVal toScVal() {
    final fields = <XdrSCMapEntry>[];
    fields.add(XdrSCMapEntry(
      XdrSCVal.forSymbol('def'),
      XdrSCVal.forU32(def),
    ));
    return XdrSCVal.forMap(fields);
  }

  factory BindingsSpecTestContractTrue.fromScVal(XdrSCVal val) {
    final map = val.map!;
    final fieldsMap = <String, XdrSCVal>{};
    for (final entry in map) {
      fieldsMap[entry.key.sym!.toString()] = entry.val;
    }

    return BindingsSpecTestContractTrue(
      def: fieldsMap["def"]!.u32!.uint32,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BindingsSpecTestContractTrue &&
          def == other.def;

  @override
  int get hashCode => Object.hashAll([
      def
  ]);
}

/// BindingsSpecTestContractError error enum
enum BindingsSpecTestContractError {
  Numbermustbeodd(1);

  final int value;

  const BindingsSpecTestContractError(this.value);

  factory BindingsSpecTestContractError.fromValue(int value) {
    return BindingsSpecTestContractError.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown BindingsSpecTestContractError value: $value'),
    );
  }

  XdrSCVal toScVal() {
    return XdrSCVal.forU32(value);
  }

  static BindingsSpecTestContractError fromScVal(XdrSCVal val) {
    return BindingsSpecTestContractError.fromValue(val.u32!.uint32);
  }
}

/// BindingsSpecTestContractFalse error enum
enum BindingsSpecTestContractFalse {
  Elif(1);

  final int value;

  const BindingsSpecTestContractFalse(this.value);

  factory BindingsSpecTestContractFalse.fromValue(int value) {
    return BindingsSpecTestContractFalse.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown BindingsSpecTestContractFalse value: $value'),
    );
  }

  XdrSCVal toScVal() {
    return XdrSCVal.forU32(value);
  }

  static BindingsSpecTestContractFalse fromScVal(XdrSCVal val) {
    return BindingsSpecTestContractFalse.fromValue(val.u32!.uint32);
  }
}

/// BindingsSpecTestContractimport_ enum
enum BindingsSpecTestContractimport_ {
  Not(11),
  Elif(12);

  final int value;

  const BindingsSpecTestContractimport_(this.value);

  factory BindingsSpecTestContractimport_.fromValue(int value) {
    return BindingsSpecTestContractimport_.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown BindingsSpecTestContractimport_ value: $value'),
    );
  }

  XdrSCVal toScVal() {
    return XdrSCVal.forU32(value);
  }

  static BindingsSpecTestContractimport_ fromScVal(XdrSCVal val) {
    return BindingsSpecTestContractimport_.fromValue(val.u32!.uint32);
  }
}

/// BindingsSpecTestContractRoyalCard enum
enum BindingsSpecTestContractRoyalCard {
  Jack(11),
  Queen(12),
  King(13);

  final int value;

  const BindingsSpecTestContractRoyalCard(this.value);

  factory BindingsSpecTestContractRoyalCard.fromValue(int value) {
    return BindingsSpecTestContractRoyalCard.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown BindingsSpecTestContractRoyalCard value: $value'),
    );
  }

  XdrSCVal toScVal() {
    return XdrSCVal.forU32(value);
  }

  static BindingsSpecTestContractRoyalCard fromScVal(XdrSCVal val) {
    return BindingsSpecTestContractRoyalCard.fromValue(val.u32!.uint32);
  }
}

/// Kind enum for BindingsSpecTestContractSimpleEnum
enum BindingsSpecTestContractSimpleEnumKind {
  First('First'),
  Second('Second'),
  Third('Third');

  final String value;

  const BindingsSpecTestContractSimpleEnumKind(this.value);

  factory BindingsSpecTestContractSimpleEnumKind.fromValue(String value) {
    return BindingsSpecTestContractSimpleEnumKind.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown BindingsSpecTestContractSimpleEnumKind value: $value'),
    );
  }
}

/// BindingsSpecTestContractSimpleEnum union
class BindingsSpecTestContractSimpleEnum {
  final BindingsSpecTestContractSimpleEnumKind kind;

  const BindingsSpecTestContractSimpleEnum._({
    required this.kind,
  });
  factory BindingsSpecTestContractSimpleEnum.first() {
    return BindingsSpecTestContractSimpleEnum._(kind: BindingsSpecTestContractSimpleEnumKind.First);
  }
  factory BindingsSpecTestContractSimpleEnum.second() {
    return BindingsSpecTestContractSimpleEnum._(kind: BindingsSpecTestContractSimpleEnumKind.Second);
  }
  factory BindingsSpecTestContractSimpleEnum.third() {
    return BindingsSpecTestContractSimpleEnum._(kind: BindingsSpecTestContractSimpleEnumKind.Third);
  }

  XdrSCVal toScVal() {
    switch (kind) {
      case BindingsSpecTestContractSimpleEnumKind.First:
        return XdrSCVal.forVec([XdrSCVal.forSymbol(kind.value)]);
      case BindingsSpecTestContractSimpleEnumKind.Second:
        return XdrSCVal.forVec([XdrSCVal.forSymbol(kind.value)]);
      case BindingsSpecTestContractSimpleEnumKind.Third:
        return XdrSCVal.forVec([XdrSCVal.forSymbol(kind.value)]);
    }
  }

  factory BindingsSpecTestContractSimpleEnum.fromScVal(XdrSCVal val) {
    final vec = val.vec!;
    final kind = BindingsSpecTestContractSimpleEnumKind.fromValue(vec[0].sym!.toString());

    switch (kind) {
      case BindingsSpecTestContractSimpleEnumKind.First:
        return BindingsSpecTestContractSimpleEnum.first();
      case BindingsSpecTestContractSimpleEnumKind.Second:
        return BindingsSpecTestContractSimpleEnum.second();
      case BindingsSpecTestContractSimpleEnumKind.Third:
        return BindingsSpecTestContractSimpleEnum.third();
    }
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) return true;
    if (other is! BindingsSpecTestContractSimpleEnum) return false;
    if (kind != other.kind) return false;

    switch (kind) {
      case BindingsSpecTestContractSimpleEnumKind.First:
        return true;
      case BindingsSpecTestContractSimpleEnumKind.Second:
        return true;
      case BindingsSpecTestContractSimpleEnumKind.Third:
        return true;
    }
  }

  @override
  int get hashCode {
    switch (kind) {
      case BindingsSpecTestContractSimpleEnumKind.First:
        return kind.hashCode;
      case BindingsSpecTestContractSimpleEnumKind.Second:
        return kind.hashCode;
      case BindingsSpecTestContractSimpleEnumKind.Third:
        return kind.hashCode;
    }
  }
}

/// Kind enum for BindingsSpecTestContractComplexEnum
enum BindingsSpecTestContractComplexEnumKind {
  Struct('Struct'),
  Tuple('Tuple'),
  Enum('Enum'),
  Asset('Asset'),
  Void('Void');

  final String value;

  const BindingsSpecTestContractComplexEnumKind(this.value);

  factory BindingsSpecTestContractComplexEnumKind.fromValue(String value) {
    return BindingsSpecTestContractComplexEnumKind.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown BindingsSpecTestContractComplexEnumKind value: $value'),
    );
  }
}

/// BindingsSpecTestContractComplexEnum union
class BindingsSpecTestContractComplexEnum {
  final BindingsSpecTestContractComplexEnumKind kind;
  final BindingsSpecTestContractSimpleStruct? struct;
  final BindingsSpecTestContractTupleStruct? tuple;
  final BindingsSpecTestContractSimpleEnum? enum_;
  final (Address, BigInt)? asset;

  const BindingsSpecTestContractComplexEnum._({
    required this.kind,
    this.struct,
    this.tuple,
    this.enum_,
    this.asset,
  });
  factory BindingsSpecTestContractComplexEnum.struct(BindingsSpecTestContractSimpleStruct value) {
    return BindingsSpecTestContractComplexEnum._(
      kind: BindingsSpecTestContractComplexEnumKind.Struct,
      struct: value,
    );
  }
  factory BindingsSpecTestContractComplexEnum.tuple(BindingsSpecTestContractTupleStruct value) {
    return BindingsSpecTestContractComplexEnum._(
      kind: BindingsSpecTestContractComplexEnumKind.Tuple,
      tuple: value,
    );
  }
  factory BindingsSpecTestContractComplexEnum.enum_(BindingsSpecTestContractSimpleEnum value) {
    return BindingsSpecTestContractComplexEnum._(
      kind: BindingsSpecTestContractComplexEnumKind.Enum,
      enum_: value,
    );
  }
  factory BindingsSpecTestContractComplexEnum.asset((Address, BigInt) value) {
    return BindingsSpecTestContractComplexEnum._(
      kind: BindingsSpecTestContractComplexEnumKind.Asset,
      asset: value,
    );
  }
  factory BindingsSpecTestContractComplexEnum.void_() {
    return BindingsSpecTestContractComplexEnum._(kind: BindingsSpecTestContractComplexEnumKind.Void);
  }

  XdrSCVal toScVal() {
    switch (kind) {
      case BindingsSpecTestContractComplexEnumKind.Struct:
        return XdrSCVal.forVec([
          XdrSCVal.forSymbol(kind.value),
          struct!.toScVal(),
        ]);
      case BindingsSpecTestContractComplexEnumKind.Tuple:
        return XdrSCVal.forVec([
          XdrSCVal.forSymbol(kind.value),
          tuple!.toScVal(),
        ]);
      case BindingsSpecTestContractComplexEnumKind.Enum:
        return XdrSCVal.forVec([
          XdrSCVal.forSymbol(kind.value),
          enum_!.toScVal(),
        ]);
      case BindingsSpecTestContractComplexEnumKind.Asset:
        final tuple = asset!;
        return XdrSCVal.forVec([
          XdrSCVal.forSymbol(kind.value),
          tuple.$1.toXdrSCVal(),
          XdrSCVal.forI128BigInt(tuple.$2)
        ]);
      case BindingsSpecTestContractComplexEnumKind.Void:
        return XdrSCVal.forVec([XdrSCVal.forSymbol(kind.value)]);
    }
  }

  factory BindingsSpecTestContractComplexEnum.fromScVal(XdrSCVal val) {
    final vec = val.vec!;
    final kind = BindingsSpecTestContractComplexEnumKind.fromValue(vec[0].sym!.toString());

    switch (kind) {
      case BindingsSpecTestContractComplexEnumKind.Struct:
        return BindingsSpecTestContractComplexEnum.struct(
          BindingsSpecTestContractSimpleStruct.fromScVal(vec[1])
        );
      case BindingsSpecTestContractComplexEnumKind.Tuple:
        return BindingsSpecTestContractComplexEnum.tuple(
          BindingsSpecTestContractTupleStruct.fromScVal(vec[1])
        );
      case BindingsSpecTestContractComplexEnumKind.Enum:
        return BindingsSpecTestContractComplexEnum.enum_(
          BindingsSpecTestContractSimpleEnum.fromScVal(vec[1])
        );
      case BindingsSpecTestContractComplexEnumKind.Asset:
        return BindingsSpecTestContractComplexEnum.asset((
          Address.fromXdrSCVal(vec[1]),
          vec[2].toBigInt()!
        ));
      case BindingsSpecTestContractComplexEnumKind.Void:
        return BindingsSpecTestContractComplexEnum.void_();
    }
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) return true;
    if (other is! BindingsSpecTestContractComplexEnum) return false;
    if (kind != other.kind) return false;

    switch (kind) {
      case BindingsSpecTestContractComplexEnumKind.Struct:
        return struct == other.struct;
      case BindingsSpecTestContractComplexEnumKind.Tuple:
        return tuple == other.tuple;
      case BindingsSpecTestContractComplexEnumKind.Enum:
        return enum_ == other.enum_;
      case BindingsSpecTestContractComplexEnumKind.Asset:
        return asset == other.asset;
      case BindingsSpecTestContractComplexEnumKind.Void:
        return true;
    }
  }

  @override
  int get hashCode {
    switch (kind) {
      case BindingsSpecTestContractComplexEnumKind.Struct:
        return Object.hash(kind, struct);
      case BindingsSpecTestContractComplexEnumKind.Tuple:
        return Object.hash(kind, tuple);
      case BindingsSpecTestContractComplexEnumKind.Enum:
        return Object.hash(kind, enum_);
      case BindingsSpecTestContractComplexEnumKind.Asset:
        return Object.hash(kind, asset);
      case BindingsSpecTestContractComplexEnumKind.Void:
        return kind.hashCode;
    }
  }
}

/// BindingsSpecTestContractTupleStruct tuple struct
class BindingsSpecTestContractTupleStruct {
  final (BindingsSpecTestContractSimpleStruct, BindingsSpecTestContractSimpleEnum) value;

  const BindingsSpecTestContractTupleStruct(this.value);

  XdrSCVal toScVal() {
    return XdrSCVal.forVec([
      value.$1.toScVal(),
      value.$2.toScVal()
    ]);
  }

  factory BindingsSpecTestContractTupleStruct.fromScVal(XdrSCVal val) {
    final vec = val.vec!;
    return BindingsSpecTestContractTupleStruct((
      BindingsSpecTestContractSimpleStruct.fromScVal(vec[0]),
      BindingsSpecTestContractSimpleEnum.fromScVal(vec[1])
    ));
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BindingsSpecTestContractTupleStruct && value == other.value;

  @override
  int get hashCode => value.hashCode;
}

/// This is from the rust doc above the struct SimpleStruct
class BindingsSpecTestContractSimpleStruct {
  final int a;
  final bool b;
  final String c;

  const BindingsSpecTestContractSimpleStruct({
    required this.a,
    required this.b,
    required this.c,
  });

  XdrSCVal toScVal() {
    final fields = <XdrSCMapEntry>[];
    fields.add(XdrSCMapEntry(
      XdrSCVal.forSymbol('a'),
      XdrSCVal.forU32(a),
    ));
    fields.add(XdrSCMapEntry(
      XdrSCVal.forSymbol('b'),
      XdrSCVal.forBool(b),
    ));
    fields.add(XdrSCMapEntry(
      XdrSCVal.forSymbol('c'),
      XdrSCVal.forSymbol(c),
    ));
    return XdrSCVal.forMap(fields);
  }

  factory BindingsSpecTestContractSimpleStruct.fromScVal(XdrSCVal val) {
    final map = val.map!;
    final fieldsMap = <String, XdrSCVal>{};
    for (final entry in map) {
      fieldsMap[entry.key.sym!.toString()] = entry.val;
    }

    return BindingsSpecTestContractSimpleStruct(
      a: fieldsMap["a"]!.u32!.uint32,
      b: fieldsMap["b"]!.b!,
      c: fieldsMap["c"]!.sym!.toString(),
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BindingsSpecTestContractSimpleStruct &&
          a == other.a &&
          b == other.b &&
          c == other.c;

  @override
  int get hashCode => Object.hashAll([
      a,
      b,
      c
  ]);
}

/// Client for interacting with the BindingsSpecTestContract contract
class BindingsSpecTestContract {
  /// The underlying SorobanClient instance
  final SorobanClient _client;

  /// Creates a new BindingsSpecTestContract for the given contract ID
  static Future<BindingsSpecTestContract> forContractId({
    required KeyPair sourceAccountKeyPair,
    required String contractId,
    required Network network,
    required String rpcUrl,
    bool enableServerLogging = false,
  }) async {
    final options = ClientOptions(
      sourceAccountKeyPair: sourceAccountKeyPair,
      contractId: contractId,
      network: network,
      rpcUrl: rpcUrl,
      enableServerLogging: enableServerLogging,
    );

    final client = await SorobanClient.forClientOptions(options: options);
    return BindingsSpecTestContract._(client);
  }

  /// Private constructor that wraps a SorobanClient
  BindingsSpecTestContract._(this._client);

  /// Gets the contract ID
  String getContractId() => _client.getContractId();

  /// Gets the client options
  ClientOptions getOptions() => _client.getOptions();

  /// Gets the contract specification
  ContractSpec getContractSpec() => _client.getContractSpec();

  /// Invokes the i32 method
  Future<int> i32({
    required int i32,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forI32(i32),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'i32',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.i32!.int32;
  }

  /// Builds an AssembledTransaction for the i32 method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildI32Tx({
    required int i32,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forI32(i32),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'i32',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the i64 method
  Future<BigInt> i64({
    required BigInt i64,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forI64(i64),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'i64',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.i64!.int64;
  }

  /// Builds an AssembledTransaction for the i64 method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildI64Tx({
    required BigInt i64,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forI64(i64),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'i64',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the map method
  Future<Map<int, bool>> map({
    required Map<int, bool> map,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forMap((map.entries.toList()..sort((a, b) => a.key.compareTo(b.key))).map((e) => XdrSCMapEntry(XdrSCVal.forU32(e.key), XdrSCVal.forBool(e.value))).toList()),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'map',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return Map.fromEntries(result.map!.map((e) => MapEntry(e.key.u32!.uint32, e.val.b!)));
  }

  /// Builds an AssembledTransaction for the map method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildMapTx({
    required Map<int, bool> map,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forMap((map.entries.toList()..sort((a, b) => a.key.compareTo(b.key))).map((e) => XdrSCMapEntry(XdrSCVal.forU32(e.key), XdrSCVal.forBool(e.value))).toList()),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'map',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Negates a boolean value
  Future<bool> not({
    required bool boolean,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forBool(boolean),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'not',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.b!;
  }

  /// Builds an AssembledTransaction for the not method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildNotTx({
    required bool boolean,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forBool(boolean),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'not',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the u32 method
  Future<int> u32({
    required int u32,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forU32(u32),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'u32',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.u32!.uint32;
  }

  /// Builds an AssembledTransaction for the u32 method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildU32Tx({
    required int u32,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forU32(u32),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'u32',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the u64 method
  Future<BigInt> u64({
    required BigInt u64,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forU64(u64),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'u64',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.u64!.uint64;
  }

  /// Builds an AssembledTransaction for the u64 method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildU64Tx({
    required BigInt u64,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forU64(u64),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'u64',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the val method
  Future<XdrSCVal> val({
    required int a,
    required XdrSCVal b,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forU32(a),
      b,
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'val',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result;
  }

  /// Builds an AssembledTransaction for the val method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildValTx({
    required int a,
    required XdrSCVal b,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forU32(a),
      b,
    ];

    return await _client.buildInvokeMethodTx(
      name: 'val',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the vec method
  Future<List<int>> vec({
    required List<int> vec,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forVec(vec.map((e) => XdrSCVal.forU32(e)).toList()),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'vec',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.vec!.map((e) => e.u32!.uint32).toList();
  }

  /// Builds an AssembledTransaction for the vec method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildVecTx({
    required List<int> vec,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forVec(vec.map((e) => XdrSCVal.forU32(e)).toList()),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'vec',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the card method
  Future<BindingsSpecTestContractRoyalCard> card({
    required BindingsSpecTestContractRoyalCard card,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      card.toScVal(),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'card',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return BindingsSpecTestContractRoyalCard.fromScVal(result);
  }

  /// Builds an AssembledTransaction for the card method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildCardTx({
    required BindingsSpecTestContractRoyalCard card,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      card.toScVal(),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'card',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the from method
  Future<String> from({
    required String finally_,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forSymbol(finally_),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'from',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.sym!.toString();
  }

  /// Builds an AssembledTransaction for the from method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildFromTx({
    required String finally_,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forSymbol(finally_),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'from',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the i128 method
  Future<BigInt> i128({
    required BigInt i128,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forI128BigInt(i128),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'i128',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.toBigInt()!;
  }

  /// Builds an AssembledTransaction for the i128 method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildI128Tx({
    required BigInt i128,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forI128BigInt(i128),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'i128',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the i256 method
  Future<BigInt> i256({
    required BigInt i256,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forI256BigInt(i256),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'i256',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.toBigInt()!;
  }

  /// Builds an AssembledTransaction for the i256 method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildI256Tx({
    required BigInt i256,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forI256BigInt(i256),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'i256',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the u128 method
  Future<BigInt> u128({
    required BigInt u128,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forU128BigInt(u128),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'u128',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.toBigInt()!;
  }

  /// Builds an AssembledTransaction for the u128 method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildU128Tx({
    required BigInt u128,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forU128BigInt(u128),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'u128',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the u256 method
  Future<BigInt> u256({
    required BigInt u256,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forU256BigInt(u256),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'u256',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.toBigInt()!;
  }

  /// Builds an AssembledTransaction for the u256 method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildU256Tx({
    required BigInt u256,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forU256BigInt(u256),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'u256',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the void_ method
  Future<void> void_({
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    await _client.invokeMethod(
      name: 'void',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );
  }

  /// Builds an AssembledTransaction for the void_ method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildVoidTx({
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
    ];

    return await _client.buildInvokeMethodTx(
      name: 'void',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the hello method
  Future<String> hello({
    required String hello,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forSymbol(hello),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'hello',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.sym!.toString();
  }

  /// Builds an AssembledTransaction for the hello method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildHelloTx({
    required String hello,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forSymbol(hello),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'hello',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the tuple method
  Future<(String, int)> tuple({
    required (String, int) tuple,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forVec([XdrSCVal.forSymbol(tuple.$1), XdrSCVal.forU32(tuple.$2)]),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'tuple',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return (result.vec![0].sym!.toString(), result.vec![1].u32!.uint32);
  }

  /// Builds an AssembledTransaction for the tuple method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildTupleTx({
    required (String, int) tuple,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forVec([XdrSCVal.forSymbol(tuple.$1), XdrSCVal.forU32(tuple.$2)]),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'tuple',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the bytes_ method
  Future<Uint8List> bytes({
    required Uint8List bytes,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forBytes(bytes),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'bytes_',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.bytes!.sCBytes;
  }

  /// Builds an AssembledTransaction for the bytes_ method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildBytesTx({
    required Uint8List bytes,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forBytes(bytes),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'bytes_',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Example of an optional argument
  Future<int?> option({
    required int? option,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      option == null ? XdrSCVal.forVoid() : XdrSCVal.forU32(option),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'option',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.discriminant == XdrSCValType.SCV_VOID ? null : result.u32!.uint32;
  }

  /// Builds an AssembledTransaction for the option method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildOptionTx({
    required int? option,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      option == null ? XdrSCVal.forVoid() : XdrSCVal.forU32(option),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'option',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the simple method
  Future<BindingsSpecTestContractSimpleEnum> simple({
    required BindingsSpecTestContractSimpleEnum simple,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      simple.toScVal(),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'simple',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return BindingsSpecTestContractSimpleEnum.fromScVal(result);
  }

  /// Builds an AssembledTransaction for the simple method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildSimpleTx({
    required BindingsSpecTestContractSimpleEnum simple,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      simple.toScVal(),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'simple',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the string method
  Future<String> string({
    required String string,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forString(string),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'string',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.str!;
  }

  /// Builds an AssembledTransaction for the string method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildStringTx({
    required String string,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forString(string),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'string',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the strukt method
  Future<BindingsSpecTestContractSimpleStruct> strukt({
    required BindingsSpecTestContractSimpleStruct strukt,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      strukt.toScVal(),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'strukt',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return BindingsSpecTestContractSimpleStruct.fromScVal(result);
  }

  /// Builds an AssembledTransaction for the strukt method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildStruktTx({
    required BindingsSpecTestContractSimpleStruct strukt,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      strukt.toScVal(),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'strukt',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the address method
  Future<Address> address({
    required Address address,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      address.toXdrSCVal(),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'address',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return Address.fromXdrSCVal(result);
  }

  /// Builds an AssembledTransaction for the address method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildAddressTx({
    required Address address,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      address.toXdrSCVal(),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'address',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the boolean method
  Future<bool> boolean({
    required bool boolean,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forBool(boolean),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'boolean',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.b!;
  }

  /// Builds an AssembledTransaction for the boolean method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildBooleanTx({
    required bool boolean,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forBool(boolean),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'boolean',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the bytes_n method
  Future<Uint8List> bytesN({
    required Uint8List bytesN,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forBytes(bytesN),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'bytes_n',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.bytes!.sCBytes;
  }

  /// Builds an AssembledTransaction for the bytes_n method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildBytesNTx({
    required Uint8List bytesN,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forBytes(bytesN),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'bytes_n',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the complex method
  Future<BindingsSpecTestContractComplexEnum> complex({
    required BindingsSpecTestContractComplexEnum complex,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      complex.toScVal(),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'complex',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return BindingsSpecTestContractComplexEnum.fromScVal(result);
  }

  /// Builds an AssembledTransaction for the complex method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildComplexTx({
    required BindingsSpecTestContractComplexEnum complex,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      complex.toScVal(),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'complex',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the duration method
  Future<BigInt> duration({
    required BigInt duration,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forDuration(duration),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'duration',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.duration!.uint64;
  }

  /// Builds an AssembledTransaction for the duration method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildDurationTx({
    required BigInt duration,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forDuration(duration),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'duration',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the timepoint method
  Future<BigInt> timepoint({
    required BigInt timepoint,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forTimepoint(timepoint),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'timepoint',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.timepoint!.uint64;
  }

  /// Builds an AssembledTransaction for the timepoint method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildTimepointTx({
    required BigInt timepoint,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forTimepoint(timepoint),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'timepoint',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the multi_args method
  Future<int> multiArgs({
    required int a,
    required bool b,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forU32(a),
      XdrSCVal.forBool(b),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'multi_args',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.u32!.uint32;
  }

  /// Builds an AssembledTransaction for the multi_args method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildMultiArgsTx({
    required int a,
    required bool b,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forU32(a),
      XdrSCVal.forBool(b),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'multi_args',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Example contract method which takes a struct
  Future<List<String>> struktHel({
    required BindingsSpecTestContractSimpleStruct strukt,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      strukt.toScVal(),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'strukt_hel',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.vec!.map((e) => e.sym!.toString()).toList();
  }

  /// Builds an AssembledTransaction for the strukt_hel method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildStruktHelTx({
    required BindingsSpecTestContractSimpleStruct strukt,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      strukt.toScVal(),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'strukt_hel',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the empty_tuple method
  Future<void> emptyTuple({
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    await _client.invokeMethod(
      name: 'empty_tuple',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );
  }

  /// Builds an AssembledTransaction for the empty_tuple method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildEmptyTupleTx({
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
    ];

    return await _client.buildInvokeMethodTx(
      name: 'empty_tuple',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the tuple_strukt method
  Future<BindingsSpecTestContractTupleStruct> tupleStrukt({
    required BindingsSpecTestContractTupleStruct tupleStrukt,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      tupleStrukt.toScVal(),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'tuple_strukt',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return BindingsSpecTestContractTupleStruct.fromScVal(result);
  }

  /// Builds an AssembledTransaction for the tuple_strukt method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildTupleStruktTx({
    required BindingsSpecTestContractTupleStruct tupleStrukt,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      tupleStrukt.toScVal(),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'tuple_strukt',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the muxed_address method
  Future<Address> muxedAddress({
    required Address address,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      address.toXdrSCVal(),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'muxed_address',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return Address.fromXdrSCVal(result);
  }

  /// Builds an AssembledTransaction for the muxed_address method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildMuxedAddressTx({
    required Address address,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      address.toXdrSCVal(),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'muxed_address',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the u32_fail_on_even method
  Future<int> u32FailOnEven({
    required int u32,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forU32(u32),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'u32_fail_on_even',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.u32!.uint32;
  }

  /// Builds an AssembledTransaction for the u32_fail_on_even method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildU32FailOnEvenTx({
    required int u32,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forU32(u32),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'u32_fail_on_even',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the tuple_strukt_nested method
  Future<(BindingsSpecTestContractSimpleStruct, BindingsSpecTestContractSimpleEnum)> tupleStruktNested({
    required (BindingsSpecTestContractSimpleStruct, BindingsSpecTestContractSimpleEnum) tupleStrukt,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forVec([tupleStrukt.$1.toScVal(), tupleStrukt.$2.toScVal()]),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'tuple_strukt_nested',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return (BindingsSpecTestContractSimpleStruct.fromScVal(result.vec![0]), BindingsSpecTestContractSimpleEnum.fromScVal(result.vec![1]));
  }

  /// Builds an AssembledTransaction for the tuple_strukt_nested method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildTupleStruktNestedTx({
    required (BindingsSpecTestContractSimpleStruct, BindingsSpecTestContractSimpleEnum) tupleStrukt,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forVec([tupleStrukt.$1.toScVal(), tupleStrukt.$2.toScVal()]),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'tuple_strukt_nested',
      args: args,
      methodOptions: methodOptions,
    );
  }
}
