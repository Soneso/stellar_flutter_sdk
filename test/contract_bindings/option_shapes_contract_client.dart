//
// This file was generated by stellar_contract_bindings v0.5.0b0
// and stellar_sdk v13.0.0.
//
// This code requires stellar_flutter_sdk (Soneso) v3.0.3 or later.
//
// @generated
//

import 'package:stellar_flutter_sdk/stellar_flutter_sdk.dart';

/// Kind enum for OptionShapesContractOptUnion
enum OptionShapesContractOptUnionKind {
  Nothing('Nothing'),
  Maybe('Maybe');

  final String value;

  const OptionShapesContractOptUnionKind(this.value);

  factory OptionShapesContractOptUnionKind.fromValue(String value) {
    return OptionShapesContractOptUnionKind.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown OptionShapesContractOptUnionKind value: $value'),
    );
  }
}

/// OptionShapesContractOptUnion union
class OptionShapesContractOptUnion {
  final OptionShapesContractOptUnionKind kind;
  final int? maybe;

  const OptionShapesContractOptUnion._({
    required this.kind,
    this.maybe,
  });
  factory OptionShapesContractOptUnion.nothing() {
    return OptionShapesContractOptUnion._(kind: OptionShapesContractOptUnionKind.Nothing);
  }
  factory OptionShapesContractOptUnion.maybe(int? value) {
    return OptionShapesContractOptUnion._(
      kind: OptionShapesContractOptUnionKind.Maybe,
      maybe: value,
    );
  }

  XdrSCVal toScVal() {
    switch (kind) {
      case OptionShapesContractOptUnionKind.Nothing:
        return XdrSCVal.forVec([XdrSCVal.forSymbol(kind.value)]);
      case OptionShapesContractOptUnionKind.Maybe:
        return XdrSCVal.forVec([
          XdrSCVal.forSymbol(kind.value),
          maybe == null ? XdrSCVal.forVoid() : XdrSCVal.forU32(maybe!),
        ]);
    }
  }

  factory OptionShapesContractOptUnion.fromScVal(XdrSCVal val) {
    final vec = val.vec!;
    final kind = OptionShapesContractOptUnionKind.fromValue(vec[0].sym!.toString());

    switch (kind) {
      case OptionShapesContractOptUnionKind.Nothing:
        return OptionShapesContractOptUnion.nothing();
      case OptionShapesContractOptUnionKind.Maybe:
        return OptionShapesContractOptUnion.maybe(
          vec[1].discriminant == XdrSCValType.SCV_VOID ? null : vec[1].u32!.uint32
        );
    }
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) return true;
    if (other is! OptionShapesContractOptUnion) return false;
    if (kind != other.kind) return false;

    switch (kind) {
      case OptionShapesContractOptUnionKind.Nothing:
        return true;
      case OptionShapesContractOptUnionKind.Maybe:
        return maybe == other.maybe;
    }
  }

  @override
  int get hashCode {
    switch (kind) {
      case OptionShapesContractOptUnionKind.Nothing:
        return kind.hashCode;
      case OptionShapesContractOptUnionKind.Maybe:
        return Object.hash(kind, maybe);
    }
  }
}

/// OptionShapesContractMaybeStruct struct
class OptionShapesContractMaybeStruct {
  final int flag;
  final int? maybe;

  const OptionShapesContractMaybeStruct({
    required this.flag,
    required this.maybe,
  });

  XdrSCVal toScVal() {
    final fields = <XdrSCMapEntry>[];
    fields.add(XdrSCMapEntry(
      XdrSCVal.forSymbol('flag'),
      XdrSCVal.forU32(flag),
    ));
    fields.add(XdrSCMapEntry(
      XdrSCVal.forSymbol('maybe'),
      maybe == null ? XdrSCVal.forVoid() : XdrSCVal.forU32(maybe!),
    ));
    return XdrSCVal.forMap(fields);
  }

  factory OptionShapesContractMaybeStruct.fromScVal(XdrSCVal val) {
    final map = val.map!;
    final fieldsMap = <String, XdrSCVal>{};
    for (final entry in map) {
      fieldsMap[entry.key.sym!.toString()] = entry.val;
    }

    return OptionShapesContractMaybeStruct(
      flag: fieldsMap["flag"]!.u32!.uint32,
      maybe: fieldsMap["maybe"]!.discriminant == XdrSCValType.SCV_VOID ? null : fieldsMap["maybe"]!.u32!.uint32,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is OptionShapesContractMaybeStruct &&
          flag == other.flag &&
          maybe == other.maybe;

  @override
  int get hashCode => Object.hashAll([
      flag,
      maybe
  ]);
}

/// Client for interacting with the OptionShapesContract contract
class OptionShapesContract {
  /// The underlying SorobanClient instance
  final SorobanClient _client;

  /// Creates a new OptionShapesContract for the given contract ID
  static Future<OptionShapesContract> forContractId({
    required KeyPair sourceAccountKeyPair,
    required String contractId,
    required Network network,
    required String rpcUrl,
    bool enableServerLogging = false,
  }) async {
    final options = ClientOptions(
      sourceAccountKeyPair: sourceAccountKeyPair,
      contractId: contractId,
      network: network,
      rpcUrl: rpcUrl,
      enableServerLogging: enableServerLogging,
    );

    final client = await SorobanClient.forClientOptions(options: options);
    return OptionShapesContract._(client);
  }

  /// Private constructor that wraps a SorobanClient
  OptionShapesContract._(this._client);

  /// Gets the contract ID
  String getContractId() => _client.getContractId();

  /// Gets the client options
  ClientOptions getOptions() => _client.getOptions();

  /// Gets the contract specification
  ContractSpec getContractSpec() => _client.getContractSpec();

  /// Invokes the default_ method
  Future<int> default_({
    required int value,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forU32(value),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'default',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return result.u32!.uint32;
  }

  /// Builds an AssembledTransaction for the default_ method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildDefaultTx({
    required int value,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forU32(value),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'default',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the opt_map method
  Future<Map<int, int?>> optMap({
    required Map<int, int?> map,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forMap((map.entries.toList()..sort((a, b) => a.key.compareTo(b.key))).map((e) => XdrSCMapEntry(XdrSCVal.forU32(e.key), e.value == null ? XdrSCVal.forVoid() : XdrSCVal.forU32(e.value!))).toList()),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'opt_map',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return Map.fromEntries(result.map!.map((e) => MapEntry(e.key.u32!.uint32, e.val.discriminant == XdrSCValType.SCV_VOID ? null : e.val.u32!.uint32)));
  }

  /// Builds an AssembledTransaction for the opt_map method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildOptMapTx({
    required Map<int, int?> map,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forMap((map.entries.toList()..sort((a, b) => a.key.compareTo(b.key))).map((e) => XdrSCMapEntry(XdrSCVal.forU32(e.key), e.value == null ? XdrSCVal.forVoid() : XdrSCVal.forU32(e.value!))).toList()),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'opt_map',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the opt_tuple method
  Future<(int?, int)> optTuple({
    required (int?, int) tuple,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forVec([tuple.$1 == null ? XdrSCVal.forVoid() : XdrSCVal.forU32(tuple.$1!), XdrSCVal.forU32(tuple.$2)]),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'opt_tuple',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return (result.vec![0].discriminant == XdrSCValType.SCV_VOID ? null : result.vec![0].u32!.uint32, result.vec![1].u32!.uint32);
  }

  /// Builds an AssembledTransaction for the opt_tuple method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildOptTupleTx({
    required (int?, int) tuple,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      XdrSCVal.forVec([tuple.$1 == null ? XdrSCVal.forVoid() : XdrSCVal.forU32(tuple.$1!), XdrSCVal.forU32(tuple.$2)]),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'opt_tuple',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the opt_union method
  Future<OptionShapesContractOptUnion> optUnion({
    required OptionShapesContractOptUnion union,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      union.toScVal(),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'opt_union',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return OptionShapesContractOptUnion.fromScVal(result);
  }

  /// Builds an AssembledTransaction for the opt_union method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildOptUnionTx({
    required OptionShapesContractOptUnion union,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      union.toScVal(),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'opt_union',
      args: args,
      methodOptions: methodOptions,
    );
  }

  /// Invokes the opt_strukt method
  Future<OptionShapesContractMaybeStruct> optStrukt({
    required OptionShapesContractMaybeStruct strukt,
    int baseFee = 100,
    int transactionTimeout = 300,
    bool simulate = true,
    bool restore = false,
    bool force = false,
  }) async {
    final List<XdrSCVal> args = [
      strukt.toScVal(),
    ];

    final methodOptions = MethodOptions(
      fee: baseFee,
      timeoutInSeconds: transactionTimeout,
      simulate: simulate,
      restore: restore,
    );

    final result = await _client.invokeMethod(
      name: 'opt_strukt',
      args: args,
      force: force,
      methodOptions: methodOptions,
    );

    return OptionShapesContractMaybeStruct.fromScVal(result);
  }

  /// Builds an AssembledTransaction for the opt_strukt method.
  /// This is useful if you need to manipulate the transaction before signing and sending.
  Future<AssembledTransaction> buildOptStruktTx({
    required OptionShapesContractMaybeStruct strukt,
    MethodOptions? methodOptions,
  }) async {
    final List<XdrSCVal> args = [
      strukt.toScVal(),
    ];

    return await _client.buildInvokeMethodTx(
      name: 'opt_strukt',
      args: args,
      methodOptions: methodOptions,
    );
  }
}
