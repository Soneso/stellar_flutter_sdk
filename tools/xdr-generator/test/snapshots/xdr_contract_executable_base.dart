// Copyright 2026 The Stellar Flutter SDK Authors. All rights reserved.
// Use of this source code is governed by a license that can be
// found in the LICENSE file.
// This file is automatically generated by xdrgen. Do not edit manually.

import 'dart:convert';
import 'dart:typed_data';

import 'txrep_helper.dart';
import 'xdr_contract_executable_external_ref.dart';
import 'xdr_contract_executable_type.dart';
import 'xdr_data_io.dart';
import 'xdr_hash.dart';
import 'xdr_json_helper.dart';

class XdrContractExecutableBase {
  XdrContractExecutableType _type;

  XdrContractExecutableType get discriminant => this._type;

  set discriminant(XdrContractExecutableType value) => this._type = value;

  /// Alias for [discriminant], the original XDR field name.
  XdrContractExecutableType get type => this._type;
  set type(XdrContractExecutableType value) => this._type = value;

  XdrHash? _wasmHash;

  XdrHash? get wasmHash => this._wasmHash;

  XdrContractExecutableExternalRef? _externalRef;

  XdrContractExecutableExternalRef? get externalRef => this._externalRef;

  XdrContractExecutableBase(this._type);

  set wasmHash(XdrHash? value) => this._wasmHash = value;

  set externalRef(XdrContractExecutableExternalRef? value) => this._externalRef = value;

  static void encode(XdrDataOutputStream stream, XdrContractExecutableBase encodedContractExecutable) {
    stream.writeInt(encodedContractExecutable.discriminant.value);
    switch (encodedContractExecutable.discriminant) {
      case XdrContractExecutableType.CONTRACT_EXECUTABLE_WASM:
        XdrHash.encode(stream, encodedContractExecutable._wasmHash!);
        break;
      case XdrContractExecutableType.CONTRACT_EXECUTABLE_STELLAR_ASSET:
        break;
      case XdrContractExecutableType.CONTRACT_EXECUTABLE_EXTERNAL_REF:
        XdrContractExecutableExternalRef.encode(stream, encodedContractExecutable._externalRef!);
        break;
      default:
        break;
    }
  }

  static XdrContractExecutableBase decode(XdrDataInputStream stream) {
    return decodeAs(stream, XdrContractExecutableBase.new);
  }

  static T decodeAs<T extends XdrContractExecutableBase>(
    XdrDataInputStream stream,
    T Function(XdrContractExecutableType) constructor,
  ) {
    T decoded = constructor(XdrContractExecutableType.decode(stream));
    switch (decoded.discriminant) {
      case XdrContractExecutableType.CONTRACT_EXECUTABLE_WASM:
        decoded._wasmHash = XdrHash.decode(stream);
        break;
      case XdrContractExecutableType.CONTRACT_EXECUTABLE_STELLAR_ASSET:
        break;
      case XdrContractExecutableType.CONTRACT_EXECUTABLE_EXTERNAL_REF:
        decoded._externalRef = XdrContractExecutableExternalRef.decode(stream);
        break;
      default:
        break;
    }
    return decoded;
  }

  String toBase64EncodedXdrString() {
    XdrDataOutputStream xdrOutputStream = XdrDataOutputStream();
    XdrContractExecutableBase.encode(xdrOutputStream, this);
    return base64Encode(xdrOutputStream.bytes);
  }

  static XdrContractExecutableBase fromBase64EncodedXdrString(String base64Encoded) {
    Uint8List bytes = base64Decode(base64Encoded);
    return XdrContractExecutableBase.decode(XdrDataInputStream(bytes));
  }

  void toTxRep(String prefix, List<String> lines) {
    lines.add('$prefix.type: ${discriminant.enumName()}');
    switch (discriminant) {
      case XdrContractExecutableType.CONTRACT_EXECUTABLE_WASM:
        _wasmHash!.toTxRep('$prefix.wasm_hash', lines);
        break;
      case XdrContractExecutableType.CONTRACT_EXECUTABLE_STELLAR_ASSET:
        break;
      case XdrContractExecutableType.CONTRACT_EXECUTABLE_EXTERNAL_REF:
        _externalRef!.toTxRep('$prefix.external_ref', lines);
        break;
      default:
        break;
    }
  }

  static XdrContractExecutableBase fromTxRep(Map<String, String> map, String prefix) {
    XdrContractExecutableType disc = XdrContractExecutableType.fromTxRepName(TxRepHelper.getValue(map, '$prefix.type') ?? '');
    XdrContractExecutableBase result = XdrContractExecutableBase(disc);
    switch (result.discriminant) {
      case XdrContractExecutableType.CONTRACT_EXECUTABLE_WASM:
        result._wasmHash = XdrHash.fromTxRep(map, '$prefix.wasm_hash');
        break;
      case XdrContractExecutableType.CONTRACT_EXECUTABLE_STELLAR_ASSET:
        break;
      case XdrContractExecutableType.CONTRACT_EXECUTABLE_EXTERNAL_REF:
        result._externalRef = XdrContractExecutableExternalRef.fromTxRep(map, '$prefix.external_ref');
        break;
      default:
        break;
    }
    return result;
  }

  /// Returns the SEP-0051 XDR-JSON rendering of this value.
  String toXdrJson() =>
      XdrJsonHelper.encodeDocument(toXdrJsonValue(), type: 'XdrContractExecutable');

  /// Parses the SEP-0051 XDR-JSON rendering of a XdrContractExecutable.
  static XdrContractExecutableBase fromXdrJson(String json) =>
      fromXdrJsonValue(XdrJsonHelper.decodeDocument(json, type: 'XdrContractExecutable'));

  /// Returns the SEP-0051 rendering of this XdrContractExecutableBase.
  Object? toXdrJsonValue() {
    switch (discriminant.value) {
      case 0:
        return <String, Object?>{'wasm': _wasmHash!.toXdrJsonValue()};
      case 1:
        return 'stellar_asset';
      case 2:
        return <String, Object?>{'external_ref': _externalRef!.toXdrJsonValue()};
    }
    XdrJsonHelper.fail(
        'XdrContractExecutable', 'holds the unknown discriminant ${discriminant.value}');
  }

  /// Reads a XdrContractExecutableBase from its SEP-0051 rendering.
  static XdrContractExecutableBase fromXdrJsonValue(Object? value) =>
      fromXdrJsonValueAs(value, XdrContractExecutableBase.new);

  /// Reads a subclass of XdrContractExecutableBase from its SEP-0051 rendering.
  static T fromXdrJsonValueAs<T extends XdrContractExecutableBase>(
    Object? value,
    T Function(XdrContractExecutableType) constructor,
  ) {
    if (value is String) {
      switch (value) {
        case 'stellar_asset':
          return constructor(XdrContractExecutableType.CONTRACT_EXECUTABLE_STELLAR_ASSET);
      }
      XdrJsonHelper.fail('XdrContractExecutable',
          'has no arm named ${XdrJsonHelper.preview(value)}');
    }
    final MapEntry<String, Object?> arm =
        XdrJsonHelper.readSingleKeyObject(value, type: 'XdrContractExecutable');
    switch (arm.key) {
      case 'wasm':
        final T arm0 = constructor(XdrContractExecutableType.CONTRACT_EXECUTABLE_WASM);
        arm0.wasmHash = XdrHash.fromXdrJsonValue(arm.value);
        return arm0;
      case 'external_ref':
        final T arm1 = constructor(XdrContractExecutableType.CONTRACT_EXECUTABLE_EXTERNAL_REF);
        arm1.externalRef = XdrContractExecutableExternalRef.fromXdrJsonValue(arm.value);
        return arm1;
    }
    XdrJsonHelper.fail('XdrContractExecutable',
        'has no arm named ${XdrJsonHelper.preview(arm.key)}');
  }
}
